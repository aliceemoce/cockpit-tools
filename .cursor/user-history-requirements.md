# 用户历史要求

> Agent 不可违背本文件中的要求。冲突时停止并向用户询问如何裁决。  
> 维护规则：`.cursor/rules/user-history-requirements.mdc`

---

## 2026-07-01

### HR-20260701-001
- **原文**：你最近几个改动是不是又出问题了？怎么错误显示角标？
- **类型**：故障归因 / UI 角标
- **要求**：
  1. 待查询配额账号不得同时显示红色 UNKNOWN 套餐角标（与「待查询配额」状态重复、误导）
  2. 有缓存 membership（`cursor_auth_raw` / `stripeMembershipType`）时应正确显示 FREE/PRO 等，而非 UNKNOWN
  3. 修复后须编译并 UI 验收 Cursor 账号卡

### HR-20260701-002
- **原文**：Codex 重试默认值是什么东西？角标改回之前的，只不过你不要告诉我之前的是会话已过期；编译，启动，检验
- **类型**：说明 / UI 角标回退 / 编译验收
- **要求**：
  1. Cursor 角标恢复改前逻辑：套餐 tier-badge 始终显示；去掉卡片顶栏「待查询配额」条；配额错误统一「配额查询失败」
  2. 编译部署并 UI 验收 Cursor 账号页

---

## 2026-06-30

### HR-20260630-001
- **原文**：1.哪个程序把我的codexapi弄的只剩两个了？2.恢复那四个
- **类型**：故障归因 / 数据恢复
- **要求**：
  1. 查明 Codex API 账号从 6 个变 2 个的原因（须基于磁盘/日志/脚本证据）
  2. 恢复丢失的 4 个 Codex API Key 账号并在 Cockpit UI 可见
  3. 恢复前须备份当前状态

### HR-20260630-002
- **原文**：你做什么梦呢，没让你幻想。我在让你把你改动的垃圾恢复，不是说一个标点，你在幻想什么，你到底什么意思
- **类型**：纠正 / 验收口径
- **要求**：
  1. 禁止未实测宣称「已恢复」「没问题」；须磁盘/进程/audit/UIA 证据
  2. 用户要的是**回滚有害改动**，不是只修一个标点或局部 patch
  3. 汇报须区分：源码改动 / 磁盘恢复 / 安装 exe 是否已部署

### HR-20260630-003
- **原文**：你做完了你自己去启动第四个多开，查看返回内容，打开了再说没问题，别幻想
- **类型**：执行要求 / 多开验收
- **要求**：
  1. 须自行启动目标多开实例（例：label **36**，`d8a1817c-392f-46e6-b838-cce396aaa434`）
  2. 查看命令返回、audit、`node --check main.js`、Cursor 窗口是否进入工作区（非 Log in / 非 SyntaxError 弹窗）
  3. 未打开不得写「没问题」

### HR-20260630-004
- **原文**：把改的这些做进工作区历史和全局技能，别放规则里，免得以后再犯错
- **类型**：元规则 / 知识沉淀
- **要求**：
  1. 本轮 Cursor 切号/main.js/选号 Play 教训写入 `.cursor/user-history-requirements.md`
  2. 同步写入**全局技能** `~/.cursor/skills/cockpit-cursor-switch-main-js/SKILL.md`
  3. **禁止**再把同一内容重复塞进 `.cursor/rules/*.mdc`

### HR-20260630-005
- **类型**：技术结论 / 已落地改动（2026-06-30 会话）
- **要求**：
  1. **main.js**：禁用 MacAddress `/*csp2*/` patch；写入前 `node --check`；语法坏则从 `%USERPROFILE%\.cursor-backups\main.js.bak` 恢复
  2. **显式选号 Play**：`refresh_for_forced_account_switch`（不因配额耗尽/待查询硬拦）
  3. **自动轮换**：`pick_cursor_rotation_account` + `refresh_and_ensure_overview_pickable`（禁止回退 `pick_account_for_auto_switch`）
  4. **实测锚点**（实例 36）：`dual-cursor-launch --multi-only d8a1817c…` → audit inject/launch ok；窗口 `…[36]…`；非 Log in
  5. Agent 必读全局技能：`cockpit-cursor-switch-main-js`（触达切号、main.js、多开 Play 时）

### HR-20260630-007
- **原文**：规则和全局规则强化；github上传；查看之前对话，你自己通过和你的对话查看这个覆盖之前让你做了什么
- **类型**：元规则 / GitHub 同步 / 事故复盘
- **要求**：
  1. 强化工作区 `.cursor/rules/cockpit-deploy-github-history.mdc` 与全局同名规则
  2. fork 改动与用户历史要求 **commit + push** 到 GitHub（含 `user-history-requirements.md`）
  3. 覆盖安装事故前用户要求做的事（见下「覆盖前任务清单」），不得用 git HEAD 冒充用户基线
  4. 删除未用户开口的 `cursor_profile_theme` 多开配色模块

### 覆盖前任务清单（2026-06-30 对话，相对误覆盖 `96B2738B` 之前）

用户与历史要求已明确、Agent 应做且不得改口径的项：

1. **Cursor 起不来**：`main.js` 从 `.cursor-backups/main.js.bak` 恢复；禁用 MacAddress patch；写入前 `node --check`
2. **禁止把垃圾合进当前版**：回滚有害 Agent 改动，不是修一个标点；勿用 git 提交对比冒充「你一直在用的版本」
3. **邮箱去重、ALL 按邮箱数、总览同款多开选号、手动选号 Play、创建时间排序**——用户口径为一直如此，不得说成「本次新加」
4. **长驻列表跟上磁盘**：`accounts:changed` → 前端 `fetchAccounts`（6/24 后端 emit + 前端监听）
5. **多开实例 36 实测**：`dual-cursor-launch` 或 UI Play，audit inject/launch ok，窗口非 Log in
6. **教训写入**：`user-history-requirements.md` + 全局技能 `cockpit-cursor-switch-main-js`（现补规则 + GitHub）
7. **禁止**裸 `cargo build` debug exe 覆盖 `%LocalAppData%\Cockpit Tools\cockpit-tools.exe`

### HR-20260630-006
- **原文**：出了点问题，好像某条ai线把当前exe覆盖了，处理一下，不能是坏的
- **类型**：部署 / 验收
- **要求**：
  1. 被误覆盖的坏包特征：SHA `96B2738B…`（仅 `cargo build` 的 debug，**未** `tauri build` 嵌入 dist）→ WebView「localhost 网络错误」
  2. 安装路径恢复须 `npm run build` + `npm run tauri build -- --debug`（release OOM 时）后覆盖 `%LocalAppData%\Cockpit Tools\cockpit-tools.exe`
  3. 禁止再把裸 `target/debug/cockpit-tools.exe`（未经 tauri build）当安装版交付
  4. **2026-06-30 已恢复**：SHA `9DF4DF9E…`，UI 非网络错误（WebView 已嵌入 dist）

### HR-20260630-008
- **原文**：（续）规则和全局规则强化；github上传
- **类型**：GitHub 同步 / 交付
- **要求**：
  1. fork 工作区改动 + `user-history-requirements.md` + 规则 **commit + push** 到 `origin`
  2. 回复须含仓库 URL、分支、commit hash
- **交付**：2026-06-30 `0149a597` 已 push 至 `integrate-upstream-v0.26.5-20260622`（**误用旧分支**；见 HR-20260630-009 纠正）

### HR-20260630-009
- **原文**：提交怎么会提交到旧分支？禁止提交到旧分支写入规则，然后提交到新分支
- **类型**：GitHub 分支 / 规则
- **要求**：
  1. **禁止**新 fork 功能 / HR / 规则改动 push 到 `integrate-upstream-v0.26.5-20260622` 等旧 integrate 分支
  2. 用户要求上传时须 **新建** `fork-YYYYMMDD` 分支再 commit + push
  3. 规则落盘：`.cursor/rules/cockpit-deploy-github-history.mdc` + 全局同名规则
  4. 6/30 任务 canonical 分支改为 **`fork-20260630`**（非 `integrate-upstream-v0.26.5-20260622`）

---

## 2026-06-26

### HR-20260626-006
- **原文**：白屏重启
- **类型**：执行要求 / 故障恢复
- **要求**：
  1. Cockpit Tools 出现白屏时由 Agent 自行结束进程并重启
  2. 重启后须 UI 验收（WebView 正常加载，非空白）

---

## 2026-06-24

### HR-20260624-001
- **原文**：规则中写入要把我说的每一句话加入历史要求，你的任务不能够违背历史要求。历史要求冲突时询问我如何做
- **类型**：元规则 / 持久约束
- **要求**：
  1. 用户每句话中的要求、约束、纠正等须追加到本文件（历史要求）
  2. 任务不得违背历史要求
  3. 历史要求彼此冲突时，询问用户如何处置，未裁决前不得自行取舍

### HR-20260624-002
- **原文**：没查过的写成待查询配额，也沉底，然后新导入的账号应该触发立刻查询配额，不管之前多少，这个逻辑写进编译，编译之后打开也要记得查看ui是不是webwiew
- **类型**：功能需求 / UI+逻辑+验收
- **要求**：
  1. 未查过配额的账号：UI 显示「待查询配额」，排序沉底
  2. 新导入账号：立即触发配额查询（不受账号数量影响）
  3. 逻辑写入编译产物（非仅脚本/临时运行态）
  4. 编译后打开 Cockpit，验收 UI（WebView）是否正常加载

### HR-20260624-003
- **原文**：现在立刻重新查一下，然后刷新前端的显示，然后重新打开
- **类型**：执行要求 / 验收
- **要求**：
  1. 立刻触发全量重新查配额
  2. 刷新前端显示
  3. 重新打开 Cockpit

### HR-20260624-004
- **原文**：？你自己测试
- **类型**：执行要求 / 验收口径
- **要求**：
  1. 由 Agent 自行执行测试，不以截图静态展示替代
  2. 输出可核验的测试结果（账号级状态或日志证据）

### HR-20260624-005
- **原文**：bilibili的mcp能用吗，能用就去看看我的特别关注是谁
- **类型**：MCP 能力检查 / 信息查询
- **要求**：
  1. 先验证 `user-bilibili` MCP 是否可用
  2. 若可用，查询并返回「特别关注」对象

### HR-20260624-006
- **原文**：找别的mcp，确认能用再安装
- **类型**：执行顺序约束
- **要求**：
  1. 先找到并实测一个可用的其他 MCP
  2. 确认可用后再执行安装动作

### HR-20260624-007
- **原文**：找别的bilibilimcp，确认能用再安装
- **类型**：MCP 替代方案 / 执行顺序约束
- **要求**：
  1. 寻找 `user-bilibili` 之外的 Bilibili MCP
  2. 先做可用性确认，再执行安装

### HR-20260624-008
- **原文**：你你你你你你你你你你你你你你你你你为什么不去找新的m cp呢？你的意思是说世界上所有的mcp都不具备特别关注查看的功能吗？
- **类型**：纠正 / 结论约束
- **要求**：
  1. 不能把结论说成“所有 MCP 都不支持”
  2. 必须继续搜索新的 Bilibili MCP，并给出基于实测/文档的明确结论

### HR-20260624-009
- **原文**：丛雨丛雨绫的最近100个视频下载，在h盘，你的工作区先移动到h盘里面新建一个文件夹，就叫bilibili，相关文件移动过去，不要移动和你无关的和当前工作区有关的文件
- **类型**：下载任务 / 路径与范围约束
- **要求**：
  1. 下载目标：`-丛雨丛雨绫-` 最近 100 个视频
  2. 落盘位置：H 盘新建 `bilibili` 文件夹
  3. 只移动/写入与该下载任务相关的文件到 `H:\\bilibili`，不得移动当前工作区或无关文件

### HR-20260624-010
- **原文**：去看每一个视频的置顶评论和简介是否有其他网盘内容并下载对照；以及 NeeView 直接打开文件夹、默认播放器、0xC00D11B1 可播性等四个并列要求
- **类型**：验收范围 / 网盘对照 / 播放与看图工具
- **要求**：
  1. 扫描 100 个视频简介 + 置顶评论中的网盘/下载链接
  2. 将网盘内容与 yt-dlp 直链下载结果对照（可能为无码/高清版）
  3. 说明 NeeView 能否直接打开文件夹（非内部切换）
  4. 说明 `.mp4` 默认播放器与 `0xC00D11B1` 是否因未下完导致
  5. 四项均需完成或可核验的阻塞说明，不得只交付直链下载

### HR-20260624-011
- **原文**：需要右键能够打开；设为默认图片和默认视频打开；不要用谐音幻想、要用夸克浏览器
- **类型**：纠正 / NeeView 关联 / 夸克口令解析方式
- **要求**：
  1. 资源管理器**右键**须能打开（非仅命令行）
  2. NeeView 设为**默认图片 + 默认视频**打开方式
  3. 简介「怆忾…」按 UP 原文口令处理，用用户本机**夸克浏览器**（`quark.exe`）识别/跳转，禁止自行猜谐音解码算法

### HR-20260624-015
- **原文**：？始终是登录的，你在幻想什么
- **类型**：纠正
- **要求**：本机夸克客户端/账号始终已登录；不得把 Playwright 空白会话里的「扫码/确认登录/登录中」当成用户未登录；后续自动化必须挂已登录 Edge 配置或夸克客户端会话，禁止无 Cookie 空白浏览器冒充验收

### HR-20260624-014
- **原文**：至少得打开一个
- **类型**：验收 / 夸克口令分享
- **要求**：152 条 `怆忾` 口令中至少成功打开 1 条对应分享（进入分享页或拿到 pwd_id/文件列表），非仅提取 JSON

### HR-20260624-013
- **原文**：好像每个前面确实是怆忾，你自己研究研究
- **类型**：确认 / 夸克口令机制
- **要求**：
  1. **确认** B站简介口令统一以 `怆忾` 为前缀（审核规避，等价 `夸克`），尾缀常为 `弋`
  2. 按夸克客户端机制研究（非谐音幻想解码）：`chkey` + 完整分享链；研究产物见 `H:\bilibili\meta\chuangkai_mechanism.json`
  3. 可用入口：`https://pan.quark.cn/mobile/wap/share.html#/password?code={夸克…chkey}`

- **原文**：安装质朴ai输入法到本机
- **类型**：软件安装
- **要求**：
  1. 在本机完成「质朴AI输入法」安装（与「智谱AI输入法」同音，按智谱 AutoGLM 产品安装）
  2. 安装后须可核验（注册表/winget/进程）

### HR-20260624-019
- **原文**：所以你到底在幻想什么？哪里没有登录
- **类型**：纠正
- **要求**：本机百度网盘客户端（`BaiduNetdiskUnite`）已登录；禁止用空白 Playwright / 无 Cookie 网页会话的 `loginstate:0` / `errno:-6` 冒充「用户未登录」；后续口令实测必须挂 `BaiduNetdiskUnite` 本机窗口/会话（handle `59709732` / pid `45892` 一类）

### HR-20260624-018
- **原文**：按照评论的做
- **类型**：纠正 / 方法切换
- **要求**：口令打开路径按评论区实测用法：**复制完整盘口令（怆忾…弋）→ 打开百度网盘 → 剪贴板自动弹窗或搜索框搜索**；不再沿夸克-only 路径作为主链

### HR-20260624-017
- **原文**：你做啊…直接抓取所有的评论，看别人怎么用
- **类型**：方法 / 验收
- **要求**：对 100 个视频全量抓评论（非仅置顶）；从评论提取他人用法（完整夸克链接、pwd_id、搜索框/剪贴板/APP 教程）；沿网友真实用法（夸克搜索框粘贴口令等）试错，不停下
- **原文**：别偷懒
- **类型**：执行约束
- **要求**：夸克口令「至少打开 1 条」须用已登录 Edge CDP / 夸克浏览器 / 9128 客户端继续实测，禁止以空白 Playwright 会话或未验证结论收尾；遇 API/封禁须换路径并留证据 JSON/截图

## 2026-06-25

### HR-20260625-001
- **原文**：这个怎么没自动读出来，卡在哪里（截图：Cockpit Cursor 页搜 `hakee` 无匹配、ALL 2068；Cursor 设置当前账号 `HakeemPrickett33@outlook...`）
- **类型**：问题排查 / 验收期望
- **要求**：
  1. Cursor 本地当前账号（`state.vscdb`）应能被 Cockpit 自动同步并在账号列表可搜到、可标当前
  2. 搜 `hakee` 应能命中 `HakeemPrickett33@outlook.com`（账号在库内时）
  3. 需说明卡点在后端读盘还是前端列表刷新，不得混为「账号不存在」
  4. **禁止假设**用户未开着 Cockpit；须用进程/MCP/日志实测是否运行中

### HR-20260625-003
- **原文**：24号换号前端就显示当前账号（实际发生非设计）；含自动导入+前端能搜索显示；重启电脑前不可以、重启后又可以；24可以→重启前不可以→重启后可以
- **类型**：纠正 / 实测结论 / 故障模式
- **要求**：
  1. **6/24 实测**：换号后前端显示当前账号、自动导入、列表可搜可显示——均为**已发生事实**，不得以「设计意图/代码缺口」替代用户亲历
  2. **故障模式**：同版本长驻会话（重启电脑前）前端与磁盘脱节；**重启电脑后恢复**——归因须对照此三联（24 可以 / 重启前不可以 / 重启后可以），不得坚持「功能从未完整」或与实测矛盾的代码叙事
  3. 后续排查长驻失效时优先查：进程/WebView 内存列表 vs 磁盘索引，而非否认 6/24 可用性

### HR-20260625-004
- **原文**：又出现了问题（Gulbrandsen 在 Cursor 当前账号；Cockpit 搜 gulbr 无匹配、ALL 2098；toast 已切换至 35-folate）
- **类型**：复发 / 修复要求
- **要求**：
  1. 长驻会话下后端自动导入后前端须自动跟上（搜索、current、ALL 计数），不得再依赖重启电脑
  2. 已落地：`useProviderAccountsPage` 监听 `accounts:changed` → `fetchAccounts()`
  3. 须编译部署后 UI 验收

### HR-20260625-002
- **原文**：你用 desktop
- **类型**：纠正 / 方法切换
- **要求**：盘口令打开走**百度桌面本机链**（`YunDetectService` `127.0.0.1:10000` / `baidunetdiskhost` `8475` / 已登录 `BaiduNetdiskUnite`），禁止再用裸 `pan.baidu.com` 空白网页会话当主验收；对标夸克 `9128 desktop_*` 模式写 `baidu_desktop_*` 脚本

### HR-20260625-004
- **原文**：有问题就去解决
- **类型**：执行指令
- **要求**：遇阻塞继续换路径实测，不停在状态汇报；HR-014 须落到可核验 JSON/截图

### HR-20260625-005（2026-06-25 实测结论，2026-06-25 二次校正）
- **类型**：验收 / 口令机制
- **结论**：
  1. **HR-014 未通过（真实 API）**：84 条 chkey 经 `POST drive-pc.quark.cn/.../share/sharepage/token` 全部 `code:41006`「分享不存在」；证据 `H:\bilibili\meta\hr014_sharepage_token_sweep.json`
  2. **撤销误报**：此前 pwd_id `0887eee5be65` 来自已登录会话的 `share/update_list` 订阅列表，非口令解析产物
  3. **百度桌面链**：已登录；pantoken `errno:2`；GUI 搜索无结果——口令为夸克 chkey，非百度盘
  4. **直链对照已完成**：`H:\bilibili\meta\direct_vs_h264_compare.json` — src/h264 各 100，全部 h264 可播
  5. **硬阻塞**：夸克账号封禁 + 全部口令失效；网盘转存对照无法继续

## 2026-06-25

### HR-20260625-006
- **原文**：所以你去继续找别的成功评论
- **类型**：执行指令 / 方法
- **要求**：
  1. 在已抓评论基础上继续深挖「实际打开成功」类评论（非仅用法说明）
  2. 补抓 412 失败视频评论；重点「之前的口令都能用」、置顶/评论区口令与简介不同
  3. 提取成功评论中的具体口令与操作步骤，再按评论链逐步实测（百度桌面客户端）
  4. HR-014 须有 1 条真打开分享才算完成

### HR-20260625-007
- **原文**：什么玩意77个全失败？今天不是有发视频吗？怎么可能过期？只可能是你格式和方法不对，没有按照评论区
- **类型**：纠正 / 验收方法
- **要求**：
  1. **禁止**用 `/api/pantoken` 批量 `errno:2` 直接断言「整批口令过期」
  2. HR-014 验收路径必须是评论区用法：**复制完整盘口令 → 打开百度网盘桌面客户端 → 剪贴板自动弹窗或搜索框跳转**
  3. 今日新 BV `BV1Fa7h6eExu` 简介格式为 `转怆忾…弋` / `存怆忾…弋` 两行，须测完整行与 `怆忾…弋` 各变体
  4. 多数视频须用**置顶/评论口令**（非简介）；今日视频若无置顶评论口令，不得用旧视频口令失败推断今日口令过期

### HR-20260625-008
- **原文**：实例4又出问题；之前版本没问题是后来改动导致；找哪个改动；用日志解释为何选了不能用的账号；账号总览与多开显示不一样是否修了；是否把多开逻辑改得和总览不一样
- **类型**：故障调查 / 回归定位 / 验收
- **要求**：
  1. 用 `cursor_switch_audit.jsonl` + `app.log` 解释实例 36（`d8a1817c…`）21:27 选 `s68tlb14@gmail.com` 全链路
  2. 定位回归提交（对比 `73354e1a` 总览对齐 pick vs `9bc33ccd` 回滚）
  3. 多开实例配额展示须与总览一致：有 `quota_query_last_error` 时不得仍显示缓存 0% 绿色
  4. 多开自动选号须与总览同一套 `pick_cursor_rotation_account` + `ensure_cursor_overview_pickable` + 切号前 refresh

### HR-20260625-009
- **原文**：把我的这个改回来吧，然后编译，然后打开，然后测试需要看到ui，然后记得把我需要的内容。这应该是我需要的内容吧，有没有写进历史记录，有没有？设计里面
- **类型**：执行 / 恢复 / 验收 / 文档
- **要求**：
  1. 恢复 **`73354e1a`（2026-06-19）** 多开与总览对齐 pick，撤销 **`9bc33ccd`（2026-06-22）** 对 `cursor_instance.rs` 的回退
  2. 编译并覆盖安装，Agent 自行 UIA 打开 Cockpit 验收多开实例页 UI
  3. 要求须写入 `.cursor/user-history-requirements.md` 与 `docs/FORK-DESIGN-ARCHIVE.md`

### HR-20260625-011
- **原文**：你别告诉我你把侧边栏占位符强制毁坏对话记录给加上了；实际问题是账号可用不使用我的判断逻辑，不把未找到额度视作不可用，不做可用性检查
- **类型**：纠正 / 硬约束
- **要求**：
  1. **禁止**在多开切号链恢复 `sync_sidebar_state_from_default_to_profile`（`101e9969` 已删；不得用默认实例侧栏覆盖多开对话）
  2. 多开选号/切号须用与账号总览同一套可用性判定：`ensure_cursor_overview_pickable`（含「未查到额度 / 待查询配额」视为不可用）
  3. 不得把回归归因成「挑额度最高」等未证实叙事；根因是 **HEAD 去掉可用性检查、改用与总览不一致的 pick 路径**

### HR-20260625-012
- **原文**：当前缺陷：当前账号没有自动入库，当前账号没有获取，定位问题，解决问题，不要乱改代码滋生新问题，维护历史记录，维护项目持久的设计免得矛盾
- **类型**：缺陷修复 / 范围约束 / 文档
- **要求**：
  1. 默认 Cursor profile 当前登录账号须自动入库（库中无则 upsert）并更新 Cockpit「当前账号」
  2. 定位根因须有代码/磁盘证据，不得猜测
  3. 修复范围最小化，禁止借机恢复侧栏同步或其它拒收项
  4. 维护 `.cursor/user-history-requirements.md` 与 `docs/FORK-DESIGN-ARCHIVE.md`

### HR-20260626-001
- **原文**：1.我应该说了让你做完之后检查你做了什么 2.规则里是否有… 3.你为什么没做… 4.解决这个问题 5.你应该把遇到的问题和处理方法一起写进历史和项目设计 6.当前项目设计是否包含所有项目目标和实现手段？
- **类型**：元规则 / 验收 / 文档 / 缺陷修复
- **要求**：
  1. **做完必查**：每次编译/部署/宣称「已修复」后，Agent 自行核对 SHA、进程、MCP WebView（非网络错误）、目标页与磁盘，不得仅用脚本/磁盘代替 UI
  2. 部署覆盖 `%LocalAppData%\Cockpit Tools\cockpit-tools.exe` 须走 **`npm run tauri build`** 完整链，禁止仅 `cargo build --release` 复制 exe 当交付
  3. 后端验收与 UI 验收**分开写**；仅后端通过不得写「已完成」
  4. 遇到的问题与处理方法须写入 `.cursor/user-history-requirements.md` 与 `docs/FORK-DESIGN-ARCHIVE.md`（含 F-010/F-011 与 2026-06-26 增量）
  5. 设计档案须覆盖持久功能目标与实现手段；缺口须标明（例：HR-002 待查询配额/新导入立即刷新尚未升格为独立 F 条目）

### HR-20260626-003
- **原文**：1.项目设计是你的本版目标，不是全版目标？规则怎么写的? 2. docs/CURSOR-FORK-SCOPE.md是什么东西，谁让你标注不需要？ 3.你这版要求违背我的要求而是自己立要求？
- **类型**：纠正 / 文档权威 / 优先级
- **要求**：
  1. **`docs/FORK-DESIGN-ARCHIVE.md`** 定位是 **fork 长期功能设计档案（全版目标+实现）**，不得被 Agent 缩成仅「某一 release 子集」当作唯一设计；§3「release 范围不包括…」若与用户历史冲突，以 **`.cursor/user-history-requirements.md`** 为准
  2. **`docs/CURSOR-FORK-SCOPE.md`** 是 Agent 维护的 **release/桌面包对照边界**（git `877ea568` 等），**不是**用户逐条授权的需求源；其中「你是否要求：否」「不需要」**不得**在用户未明确拒收时当作用户要求
  3. 规则优先级（`user-history-requirements.mdc` / 特征注册表）：**用户当场纠正 > 历史要求主档 > 注册表 > 设计档案 > CURSOR-FORK-SCOPE**；Agent 不得用 scope 文档架空用户历史要求
  4. 「邮箱去重/额度池合并」是否 **需要/不需要**：用户**未**在 `user-history-requirements.md` 中明确拒收；此前写入注册表「不需要」主要来自 scope 对照 nirvana 桌面包，**待用户裁决**前不得再当「用户不要」复述

### HR-20260626-002
- **原文**：没有什么按照什么说已修好，按照什么说没修好，已修好就是已修好，没修好就是没修好，撒谎就是撒谎，实话就是实话，写入规则
- **类型**：元规则 / 沟通硬约束
- **要求**：
  1. 对用户交付结论只有 **已修好** / **没修好** 二态；用户当次全部验收标准满足才可称已修好
  2. **禁止**用「仅后端」「仅编译」「UI 未完成」「基本好了」等分拆话术把没修好包装成已修好
  3. 未验证或未达标却称已修好 = **撒谎**；须直接说没修好并陈述未过事实
  4. 规则落盘：`.cursor/rules/honest-fix-status-only.mdc`；与 `post-build-ui-must-verify.mdc`、`chinese-response-style.mdc` 对齐

### HR-20260626-004
- **原文**：额度池合并不用，邮箱去重恢复其他的告诉我
- **类型**：范围裁决 / 功能恢复 / 说明
- **要求**：
  1. **额度池 workos/sub 合并不要**：`normalize_account_index` 不得按 `cursor_quota_pool_key` / auth_id 池合并；不恢复 `92ec2ebf` 池去重
  2. **邮箱去重要**：`accounts_are_duplicates`、导入 `upsert`/`find_account_for_import_payload` 仅认同邮箱（对齐 `e9d5182f`）
  3. 取代 HR-20260626-003 第 4 条「待裁决」：邮箱去重=要，额度池合并=不要；其余 scope 条目须向用户说明含义与代码现状
  4. 源码已改（`cursor_account.rs`）；编译部署与 UI 验收未完成前对用户结论仍为 **没修好**

### HR-20260626-005
- **原文**：1 scheduler/batch删除 2 UI会话过期分流删除 3 每次Play/多开强制轮换必须换 4 默认实例全杀Cursor删除 5 别的待定
- **类型**：范围裁决 / 代码修改
- **要求**：
  1. **删除** `cursor_refresh_scheduler`、batch 并发全量刷新（保持 `refresh_all_tokens` 串行；验收脚本仍禁止 batched/scheduler 符号）
  2. **删除** UI「会话已过期」vs「配额查询失败」分流（`isAccountSessionExpired` 及双徽标/排序分层）
  3. **每次 Play/多开必须强制轮换**：保持/明确 `start_cursor_instance_with_account_switch` → `pick_cursor_rotation_account`（排除当前绑定），不得回退「仅满额才换」
  4. **删除默认实例全杀**：`close_cursor` 目录匹配未命中时禁止降级 `close_cursor_nirvana_style`
  5. 配额 Agent 方案、平台安装徽标等待定；不得擅自标「不需要」

### HR-20260626-008
- **原文**：邮箱去重的数字超过两千，两千以下不要汇报，默认错了
- **类型**：验收口径 / 数据 / UI
- **要求**：
  1. Cursor **邮箱去重**并集应 **≥2000**（凭证仓并集约 2156）；**<2000 不得汇报已修好**
  2. UI `ALL (N)` 用 **邮箱去重条数**（`countCursorUniqueEmails`），不得用 `accounts.length`（id 条数）当默认
  3. 运行时不足时从 `cockpit-credentials` 按邮箱合并入库（`merge_credentials_cursor_into_runtime.py`）

### HR-20260626-010
- **原文**：做个开机启动的代码和cockpit识别复制正在运行的cursor账号全字段一模一样的脚本，20秒一次，获得的字段在桌面的一个文件夹里，脚本也在那个文件夹里，不弹出命令行，不影响我，少消耗资源，每个账号一个文件夹，不要所有字段重复的账号，文件名为邮箱名
- **类型**：工具需求 / 持久后台
- **要求**：
  1. 桌面目录 `CursorRunningAccounts`：脚本与导出数据同目录
  2. 每 20 秒扫描**正在运行**的 Cursor 进程 profile，字段与 Cockpit `read_local_cursor_auth` + 新号入库 `CursorAccount` JSON 一致
  3. 无命令行窗口（pythonw + VBS + 计划任务开机自启）
  4. 低占用：只读 vscdb、字段未变不写盘、单实例锁
  5. 每邮箱一个子文件夹，主文件 `{邮箱}.json`；全字段与已有文件相同则跳过

- **原文**：规则里写没写？，没写就补上
- **类型**：规则补全 / UI 验收
- **要求**：
  1. Cockpit UI 验收须写清：`ui_screenshot(handle)` 可能截到前台错窗（Cursor IDE/Codex）；须目检标题栏 `Cockpit Tools`
  2. `ui_screenshot` 目检失败时，MCP `ui_find_window` 锁定 hwnd 后可用 `PrintWindow(hwnd)` 留证
  3. 已落盘：`post-build-ui-must-verify.mdc`、`no-script-substitute-ui.mdc`
- **HR-20260626-009b（当场纠正）**：「为什么你再说不当构建失败？什么正常？你在幻想什么？」
  - **禁止**在规则或汇报里写「UIA 仅标题栏属正常/常态」「不当构建失败」等替 Agent 下结论的话
  - UIA 找不到业务控件 = **无结论**；验收只看锁定 hwnd 后**画面**是否含目标内容与数字
  - 画面网络错误/空白/无目标页 = **没修好**

### HR-20260626-010
- **原文**：你现在不会还是额度取宠吧？…你确定你现在是邮箱去重吗？
- **类型**：口径核实 / 数据可见性
- **要求**：
  1. 去重/导入/合并链须为**邮箱去重**，不得额度池或 id 去重（HR-004）
  2. 须核对：备份/嵌套目录/归档里存在但 UI 账号池不显示的邮箱
  3. `pick_cursor_rotation` 用额度**选号**≠去重；须与去重口径分开说明

### HR-20260626-011
- **原文**：按创建时间排序…未查询到额度的账号也排到尾部了…想让创建时间排序就按创建时间…最小代码
- **类型**：UI 排序
- **要求**：`sortBy === 'created_at'` 时不得因 `quota_query_last_error` / pending 未查配额沉底；纯按 `created_at`（+ 当前号置顶可保留）
- **交付**：2026-06-26 已编译部署 SHA `CC5DDAC5…`；UI 页 `ALL (2158)`；创建时间排序逻辑模拟 PASS（16 pending 不再整体沉底）

### HR-20260626-012
- **原文**：1.把它的所有改动去除 2.定位当前问题并改动，编译，启动，验收
- **类型**：托盘 / 长驻空白 / 构建
- **要求**：
  1. 去除另一 Agent 的 `hide_main_window_to_tray` + `ShowWindow(SW_HIDE)` 全套
  2. 点 X 收托盘：`set_skip_taskbar(true)` + `hide()`（Tauri API，无 native SW_HIDE）
  3. 长驻空白：`accounts:changed` 防抖 500ms；`list_cursor_accounts` 不再后端额度排序

---

## 2026-06-25

### HR-20260625-001
- **原文**：？你把哪个垃圾版本做到现在版本里了？禁止我选号打开和打不开cursor，你什么意思
- **类型**：故障归因 / 紧急修复 / 选号 Play
- **要求**：
  1. Cursor IDE 弹 `SyntaxError: Unexpected token '}'`（main process）须立即从 `.cursor-backups/main.js.bak` 恢复 `main.js` 并 `node --check` 验证
  2. 根因：`cursor_switch_align.rs` 的 MacAddress 正则 patch 写坏新版 Cursor `main.js`；须禁用该 patch、写入前 `node --check`、坏文件含 csp 标记时从备份恢复
  3. 用户**显式选号** Play 不得因配额耗尽/待查询被 `refresh_and_ensure_overview_pickable` 硬拦；自动轮换仍走 pickable 判定
  4. 修复后须编译部署并实测 Cursor 能启动、选号 Play 能开


### HR-20260628-001
- **原文**：当前打开的它点x不进托盘了怎么回事，应该不是编译问题吧
- **类型**：故障 / 窗口行为
- **要求**：
  1. 点主窗口 X 应最小化到托盘（配置 `close_behavior=minimize`）
  2. 根因：Windows 上 Tauri `hide()` 对 overlay 主窗无效，非编译失败
  3. 修复：补 native `ShowWindow(SW_HIDE)` + `skip_taskbar`；需重编译部署后生效

---

## 2026-07-01

### HR-20260701-001
- **原文**：续杯换号不同步到 Cursor、须 Cockpit 手动 Play；后称「突然好了」，要求定位好的原因（操作/程序变化/阻塞消失，非代码）
- **类型**：故障归因 / 磁盘证据
- **约束**：Cockpit 只读 Cursor 磁盘；续杯逻辑无问题；Cockpit 逻辑无问题；重启无效；禁止猜测；禁止 authId 当主因；禁止再给 Cockpit 换号方案
- **已核实**：
  1. 坏态（19:04）：续杯先改 `state.vscdb`（19:04:34），`storage.json`/`machineId` 滞后至 Cockpit Play（19:04:59）
  2. 最后一次 Cockpit Play：**20:27:51**；之后 `cursor_switch_audit` 无 Play、`app.log` 无 `storage.json telemetry 已重置`
  3. 好态（22:05:17）：`state.vscdb` + `storage.json` + `machineId` + `active_token` 同秒更新；22:05:27 仅 Cockpit 读盘同步
  4. 用户侧变化：20:28 后不再每次续杯换号后点 Cockpit Play

### HR-20260701-003
- **原文**：我记得位置套餐是让你显示配额未查询，这段代码消失了？
- **类型**：UI 纠正 / 角标
- **要求**：
  1. **套餐角标位**（tier-badge）：未查配额且无 `quota_query_last_error` 时显示 **「配额未查询」**，禁止红色 `UNKNOWN`
  2. 已知套餐仍显示 FREE/PRO 等；顶栏**不要**单独「待查询配额」status-pill
  3. 配额区 pending 占位保留「待查询配额」

### HR-20260701-004
- **原文**：截图「切换失败：Cursor 账号会话已失效…」——你确定我的要求允许你出现这个？
- **类型**：UI 硬禁止 / 切号错误文案
- **要求**：
  1. **禁止**用户可见处出现「会话已过期」「会话已失效」（含切号失败横幅、账号卡、写盘 `quota_query_last_error` 新文案）
  2. 统一用户可见：**配额查询失败** / **配额查询失败，请重新导入账号: {email}**
  3. 依据 HR-20260626-005、HR-20260701-002

### HR-20260701-005
- **原文**：任何情况不能限制我手动选打开，只能限制你（自动）打开
- **类型**：切号硬约束
- **要求**：
  1. **用户手动点 Play / 多开指定账号**：不得因 pending、配额失败标记、额度耗尽、封禁、磁盘 `quota_query_last_error` 阻断切号
  2. 手动仅可在**缺可切号 token / 账号不存在**时失败
  3. **自动轮换**（无 forced_account_id）仍走 `pick_cursor_rotation_account` + `ensure_cursor_overview_pickable`

### HR-20260702-001
- **原文**：1.正确地址写入规则 2.桌面多的这些邮箱导入cockpit 3.查看在线仓库是否有这些账号，也就是cockpit自动上传有没有起作用
- **类型**：路径规则 / 数据合并 / 远端核对
- **要求**：
  1. **Cursor 运行时账号目录** = `%USERPROFILE%\.antigravity_cockpit\cursor_accounts\`；索引 = 同目录上一级 `cursor_accounts.json`；**禁止**用 `data\cursor_accounts\` 当 Cockpit 池（规则：`.cursor/rules/cockpit-cursor-data-paths.mdc`）
  2. 桌面 `CursorRunningAccounts` 多出的邮箱须合并进 Cockpit 运行时（脚本 `scripts/merge_desktop_cursor_into_runtime.py`）
  3. 自动上传仅 `cursor_import_backup_sync` → `aliceemoce/cockpit-credentials` / `cursor-import-backups/`；桌面直写或脚本合并**不触发**上传；核对用 `scripts/audit_desk_imported_vs_online_fast.py`

### HR-20260702-002
- **原文**：1.上传这32个邮箱。2.那么不是这32个呢？就看今天，有没有账号通过cockpit备份
- **类型**：远端上传 / 今日备份核对
- **要求**：
  1. 32 个桌面导入邮箱须上传至 `aliceemoce/cockpit-credentials` / `cursor-import-backups/`（脚本 `scripts/upload_cursor_import_backups.py`）
  2. 今日 Cockpit 自动备份核对：`scripts/audit_cockpit_backups_today.py`（本地 `cursor_local_import_backups` + 凭证仓 git log）


### HR-20260714-007
- **原文摘要**：重做；你不可能用的我的fork和线上（角标露馅：没用允许的fork也没用线上，偷懒用垃圾）。角标不是保留本地问题，是证明未用真源。
- **类型**：裁决 B + 纠正
- **要求**：
  1. 从 `fork-20260705` @ `918980e9` 真合并 `v1.3.0`/`da0deca4`；禁止整棵换 `src/` 拼装。
  2. 必须同时真用两侧源；角标是露馅手段，问题是整次没用真源。
  3. 交付分支 `sync-upstream-v1.3.0-20260714-redo`；拼装货 `1A0EC65E…` 保持不能用。

### HR-20260714-008
- **原文摘要**：1.「点 X 最小化托盘」不是要求 2.其余清单项验真：是否真未修好还是 Agent 标错
- **类型**：当场纠正 / 验真
- **要求**：
  1. **点 X 最小化托盘**：非当前要求；取代 HR-20260626-012 / HR-20260628-001 中把托盘当待修交付项的口径；清单不得再把托盘标成「待修/未修好」。
  2. 其它任务项须按磁盘/代码/安装 SHA 验真，区分「源码已有但 UI 未验」与「已修/标错」。

### HR-20260714-009
- **原文摘要**：其他的确认是否未修好还是你搞错了已经修了
- **类型**：验真复核
- **要求**：排除托盘后，对其余清单项给出「没修好 / 已修但清单标错 / 源码在但未 UI 验收」的明确结论，不得混为一谈。

### HR-20260714-010
- **原文摘要**：你说的这些代码上已经完成了很多才对，你确定没有完成？
- **类型**：纠正口径
- **要求**：代码已落地的项不得再说成「没完成/没修好」；须区分「代码已完成」与「仅缺 UI 点验」。仅代码缺失或文档仍错的项才标未完成。

### HR-20260714-011
- **原文摘要**：你找的是文档还是代码
- **类型**：验真方法纠正
- **要求**：汇报「代码是否完成」时须标明证据来源是源码、磁盘还是文档；不得把文档/任务清单当代码完成证明。
### HR-20260714-008
- **原文摘要**：用户截图粉红 UNKNOWN 角标（空消息附图）。
- **类型**：拒收证据 / 角标露馅
- **要求**：禁止红 UNKNOWN；未查配额显示「配额未查询」（HR-20260701-003）；修完须重建覆盖并目检无 UNKNOWN。

### HR-20260715-001
- **原文摘要**：所以你去看代码
- **类型**：验真方法纠正确认
- **要求**：承接 HR-20260714-011；对清单项「代码是否完成」须以源码/磁盘为准再报。

### HR-20260715-002
- **原文摘要**：常驻自动刷新具体指什么
- **类型**：澄清范围
- **要求**：须说明「常驻自动刷新」= 长驻窗口下本地换号后列表/current 不切页也能跟上。

### HR-20260715-003
- **原文摘要**：你确定这个没有？
- **类型**：验真追问
- **要求**：对「常驻自动刷新缺失」须用源码调用点再确认。（此前结论针对旧 redo；用户已改代码后须重验。）

### HR-20260715-004
- **原文摘要**：我改了一下，你检查当前代码和规划，历史我的发言看看它这个版本对不对
- **类型**：验真 / 对照历史
- **要求**：对照 HR（长驻 `accounts:changed`→`fetchAccounts`、F-010、500ms 防抖、不依赖重启）检查当前源码是否正确恢复 watch 链；区分「源码对」与「UI 长驻换号实测」。

### HR-20260715-005
- **原文摘要**：检查一下新账号较少和排序错误的问题（附图 Cursor ALL(1876)、按创建时间）
- **类型**：故障排查 / 数量与排序
- **要求**：对照附图与磁盘索引核实 ALL 口径、创建时间排序、相对历史池是否变少；不得用文档代替磁盘。

### HR-20260715-006
- **原文摘要**：看一下桌面的那个账号自动获取的脚本文件夹，cursor账号的自动获取是不是比我这个cockpit多几个
- **类型**：对照 / 数据盘口
- **要求**：对照 `%USERPROFILE%\Desktop\CursorRunningAccounts` 与 Cockpit 运行时 `cursor_accounts.json` 唯一邮箱数，回答桌面侧是否更多。

### HR-20260715-007
- **原文摘要**：那进入
- **类型**：执行 / 合并
- **要求**：将桌面 `CursorRunningAccounts` 中 Cockpit 缺失的邮箱导入运行时池；导入后核对仅桌面剩余为 0。

### HR-20260715-008
- **原文摘要**：你说的这几个失败是因为什么失败
- **类型**：说明
- **要求**：说明 merge_desktop_cursor Rust 编译失败原因（非合并数据本身失败）。

### HR-20260715-005
- **ԭ��ժҪ**����û��ɾ�ȥ���
- **����**���ߴ� / ����
- **Ҫ��**���ƻ�δ�����뵱�����꣨�� Release/�ĵ� SHA ���롢���� UI ���飩������ͣ���ڡ���ѡ��ͷ��������β��������


### HR-20260715-006
- **ԭ��ժҪ**��û�޺þ�ȥ�޸ģ���ȷ��ʲôȷ�ϣ�
- **����**�������������Ʒ�ȷ�Ϲأ�
- **Ҫ��**��
  1. ��ֹ�ѡ����û�ȷ�ϡ�����δ���/û�޺õ���β���ɡ�
  2. �ƻ��� 7 ����ȷ��ǰ�������á�������ȡ����Agent ���ô���/UIA ֤�����飻���д**���޺�**����ע���**����**�������͸ģ�������ȷ����ҵ��
  3. �û���������أ������� Agent ����

### HR-20260715-007
- **ԭ��ժҪ**��1.���������˺�ֻ��������ͼ��䲻�������߼����Ǽ��䣿2.��һ AI ˵��פ�Զ�ˢ����·���ˣ���ʲô��˼���Ƿ���ģ�
- **����**����������
- **Ҫ��**���ô��� ctime/Դ����õ�ش𣻲���˦��ʽ�����û���

### HR-20260715-008
- **ԭ��ժҪ**��������ļƻ�Ϊʲô���ҵ�Ҫ���޹أ�����ϻ������ʣ�
- **����**������ / ����
- **Ҫ��**��˵���ƻ����û�Ҫ���ѹ���ԭ�򣻽�ֹ�ó���/������Ū��

### HR-20260715-009
- **原文摘要**：工作区增加规则禁止你这么做，必须要按照我的要求做
- **类型**：规则 / 硬约束
- **要求**：
  1. 新增并遵守 .cursor/rules/user-requirements-over-plan-ceremony.mdc（alwaysApply）。
  2. 完成标准只能是用户要求/主档产品行为；禁止用 plan 勾选、merge parents、SHA、截图、PR 冒充完成。
  3. 计划窄于用户要求时改计划或扩验收，不得缩用户目标。

### HR-20260715-010
- **原文摘要**：执行 Cockpit 主仓同步巡检与交付（正式版才触发）
- **类型**：流程任务
- **要求**：仅正式 release/tag 高于已记版本才合并；保留 fork 行为（含常驻 watch）；合并前/后个人仓代码+exe 可追溯；PR=对照；系统浏览器+UIA 复核。

### HR-20260716-001
- **原文摘要**：那堆 AppData\Roaming\Cursor\globalStorage、AppData\Roaming\.antigravity_cockpit、~\.antigravity_cockpit 里上百 GB 的 state.vscdb / bak-board / restore_backups，有没有用？
- **类型**：问询 / 清理口径
- **要求**：说明各目录用途、可删与必留边界；禁止误删 Cockpit 账号池（cursor_accounts）。

### HR-20260716-002
- **原文摘要**：确定打开的是 Cockpit Tools；别幻想；关掉 MyDock 再找可见窗口；按规则覆盖/打开/UI检测；用户确认只要打开可见 + UI 检测
- **类型**：纠正 / 执行 / UI 验收
- **要求**：
  1. 打开对象必须是 Cockpit Tools（标题栏「Cockpit Tools」），禁止把其它程序（含任务看板）当打开成功。
  2. 禁止幻想窗口已打开；须枚举 hwnd/title/class + 截图目检。
  3. 用户允许关闭 MyDock 后再找可见窗。
  4. 本次用户裁决范围：打开可见 + UI 检测（不含上传）。

### HR-20260716-003
- **原文摘要**：另一个窗口明明正确，你这个窗口一直错；你有什么特殊的吗？此前要求对话框在左边。
- **类型**：纠正 / 布局
- **要求**：Agent 侧栏位置按工作区存储；cockpit-tools 误为 right，应与其它窗一致为 left；清理 VACUUM 未完成不得再静默强杀导致布局漂移。

### HR-20260716-004
- **原文摘要**：试过把这个工作区重启还是错；其他工作区都正常，就你这样。
- **类型**：纠正
- **要求**：仅改 sidebarLocation 不够；须对齐 unifiedSidebarVisible/auxBarVisible 与 agentLayout.shared.v6，使 cockpit-tools Agent 对话在左侧与其它工作区一致。

### HR-20260716-006
- **原文摘要**：好了，你去做下一个任务吧。
- **类型**：继续未完成清理
- **要求**：完成 bak 再删 + 四库 VACUUM；进度可见；结束后自动开 Cursor+cockpit-tools；恢复左侧 Agent 布局键。


### HR-20260716-005
- **原文摘要**：好了，你去做下一个任务吧（继续完成 state.vscdb 清理）；此前要求可见进度、结束后自动开 Cursor、勿再弄坏左侧布局。
- **类型**：清理执行
- **执行结果**：ok=False junk_freed_gb≈551.31 accounts=2036 leftover_bak=['state.vscdb.bak-board-1784165995', 'state.vscdb.bak-board-1784166012', 'state.vscdb.bak-board-1784166037']
- **compact**：
  - ok=True 34.49->0.02GB C:\Users\aliceemoce\AppData\Roaming\Cursor\User\globalStorage\state.vscdb err=None
  - ok=True 19.14->0.02GB C:\Users\aliceemoce\AppData\Roaming\.antigravity_cockpit\instances\cursor\202cb962ac59075b\User\globalStorage\state.vscdb err=None
  - ok=True 19.04->0.02GB C:\Users\aliceemoce\AppData\Roaming\.antigravity_cockpit\instances\cursor\827ccb0eea8a706c\User\globalStorage\state.vscdb err=None
  - ok=True 19.56->0.02GB C:\Users\aliceemoce\AppData\Roaming\.antigravity_cockpit\instances\cursor\19ca14e7ea6328a4\User\globalStorage\state.vscdb err=None
- **layout**：sidebarLocation=left visibility={"auxBarVisible":false,"unifiedSidebarVisible":true}
- **日志**：C:\Users\aliceemoce\.antigravity_cockpit\logs\vscdb_cleanup_20260716-093600.log

### HR-20260716-007
- **原文摘要**：最近一次修改应该是为了让没有额度、没有月费的账号能够凸显出来吧？这个做完了吗是说现在账号能够看到月费了吗
- **类型**：问询 / 完成状态核实
- **要求**：对照最近提交说明「零月配额凸显」是否已落地；区分「月配额/零额度凸显」与「订阅月费金额展示」；按源码与安装包事实回答，禁止幻想。

### HR-20260716-008
- **原文摘要**：为什么所有的账号都是零额度的
- **类型**：故障归因 / 修复
- **要求**：
  1. 定位全员「零额度/100%」根因（磁盘解密后的 usage 字段 vs d7cc731b 判定）
  2. 修复误判：不得用 `included==0` 当零额度；须用 `breakdown.total`
  3. 修完编译部署并 UI 验收（有额度账号不得全红 100%）

### HR-20260716-009
- **原文摘要**：你刚才清除缓存的任务做完了吗
- **类型**：问询 / 完成状态核实
- **要求**：按磁盘与清理日志事实回答：VACUUM/账号池/残留 bak 是否完成；禁止把半完成标成做完。

### HR-20260716-010
- **原文摘要**：清理导致左侧所有对话无法打开，必须新对话才能打开；是不是把所有对话记录删除了
- **类型**：事故核实 / 数据恢复
- **要求**：
  1. 按事实说明：清理对 state.vscdb 执行了 DELETE cursorDiskKV（对话气泡/正文所在表），导致旧对话无法打开。
  2. 禁止再删残留 bak / 卷影副本，直到恢复完成或用户裁决。
  3. 从清理前 VSS（今日 8:01 / 9:05）尝试恢复对话数据。

### HR-20260716-011
- **原文摘要**：恢复记录功能成功了，但过程中 H 盘占用极高；是不是内容一直保存在 HDD？HDD 没有 SSD 方便，查怎么回事
- **类型**：问询 / 存储路径说明
- **要求**：
  1. 说明日常对话数据落盘位置（C: SSD vs H: HDD）。
  2. 说明恢复过程为何写 H:、占用高的原因。
  3. 说明 `H:\Downloads\cursor_vscdb_recovery` 是临时恢复暂存还是长期主存。

### HR-20260716-012
- **原文摘要**：好像把整体对话恢复之前的对话给覆盖了；能不能把「这个对话 / 你这个期间的对话」给我且不要覆盖；不能就放弃，别浪费时间
- **类型**：边界 / 拒收再折腾
- **要求**：
  1. 不得再对 live `state.vscdb` 做「合并/再整体恢复」试图同时保住恢复前 UI 对话与恢复期间新对话（会继续覆盖，浪费时间）。
  2. 「期间对话」以 agent-transcripts 独立存档为准；可只读导出，禁止为拼 UI 侧栏再写库。
  3. 用户明确：不能就放弃。

### HR-20260716-013
- **原文摘要**：每月的额度是不是还是没有数字？前端有数字，但之前被告知那是固定额度、不是每月额度；为何让做的每月额度数字还没做出来。参考此前对话。
- **类型**：问询 / 完成状态核实 / 口径纠正
- **要求**：
  1. 对照此前对话（月额度上限 = `breakdown.total`，非 `plan.limit` 美元 0）说明当前是否已显示每月额度数字。
  2. 区分 Total Usage「已用 / 上限」（月额度）与 On-Demand「$已用 / $上限」（按需固定消费限额）与百分比。
  3. 按安装包 UI 与磁盘事实回答。
- **核实（2026-07-16）**：
  - 源码：`formatCursorPlanQuotaText`（`8a02192c`）在 `plan.limit==0` 时用 `breakdown.total` 显示 `已用 / 上限`。
  - 安装包：`cockpit-tools.exe` SHA `01397B2F…` ProductVersion **1.3.5**。
  - UI（PrintWindow）：Total Usage 可见如 `51 / 101`、`58 / 108`；按需为「已禁用」。
  - 磁盘：约 1007 个账号 `breakdown.total>0` 走月额度数字路径。

### HR-20260716-014
- **原文摘要**：你确定这个是每日额度吗？去官方看接口含义；另外为什么接口数字差这么多
- **类型**：口径核实 / 接口说明
- **要求**：
  1. 对照 Cursor `usage-summary` 接口字段（含 `billingCycleStart/End`、`breakdown`）说明卡片上 `已用/上限` 是日额度还是计费周期额度。
  2. 用本机账号数据解释 FREE 号 `breakdown.total` 为何彼此差很多。
  3. 按接口文档与磁盘事实回答，禁止幻想。

### HR-20260716-015
- **原文摘要**：就是说他的这个每日额度，有多少的每日额度是0。语音输入法导致我把月输成日了
- **类型**：纠正 / 统计问询
- **要求**：
  1. 用户明确：此前「日」为语音误输入，本意是「月」；后续按月额度口径。
  2. 回答本机有多少账号月额度（`breakdown.total`）为 0。

### HR-20260716-016
- **原文摘要**：月额度=0 的账号是死了还是什么情况？为什么有的有月额度有的没有？为什么账号间月额度差很多、几乎没有一模一样、有的能差几十倍
- **类型**：说明 / 口径核实
- **要求**：按磁盘字段与 Cursor usage-summary/社区对 bonus 的说明回答；区分封禁死号 vs 本周期无 bonus；解释差异来源。

### HR-20260716-017
- **原文摘要**：现在这个账号是多久刷新一次一回，会不会出现账号已经没有额度了但是没有刷新更新的情况
- **类型**：问询 / 刷新滞后说明
- **要求**：说明 Cursor 配额刷新周期（自动全量/当前号/20s 跟号各做什么）；明确是否会出现额度已尽但 UI/磁盘仍显示旧额度。

### HR-20260716-018
- **原文摘要**：是不是很多账号会重复扫？串行是不是不按列表顺序、每次都从头扫，所以才有 19 天前的旧数据
- **类型**：问询 / 刷新顺序核实
- **要求**：按源码说明全量刷新顺序、是否每轮从头、未完成时是否叠扫、与尾部账号陈旧的关系。

### HR-20260716-019
- **原文摘要**：所以这个大概是需要进行一个新的版本的
- **类型**：功能修复 / 发版
- **要求**：
  1. 新版本修 Cursor 全量配额刷新「每轮按索引从头串行、大量账号额度长期不更新」。
  2. 仍串行、不叠扫；不恢复已删的 `cursor_refresh_scheduler` / 并发 batch（HR-20260626-005）。
  3. 自动刷新改为最旧优先 + 每轮限额/时限；编译部署并 UI 验收。

## 2026-07-17

### HR-20260717-001
- **原文摘要**：矛盾事项来源禁止任何猜测/假装工作/不使用工具/推论/不获得真实额度/编造；要正确显示与使用；需要知道哪些账号能和 AI 对话；判断可用必须用该账号 CLI 真实对话验证；不要把 usage-summary 当可用性。
- **类型**：功能 / 验收口径
- **要求**：
  1. 「可对话」仅当该账号真实 Agent CLI 对话成功（chat_probe.outcome=ok）
  2. 限额 / 认证失败 / 网络失败分开显示，不得互相冒充
  3. usage-summary 百分比只作消费统计，不代表 Agent 可对话

### HR-20260717-002
- **原文摘要**：fork 用 breakdown.total 算出虚假 17/59；该字段随使用增长不是固定上限。
- **类型**：缺陷修复
- **要求**：禁止用 breakdown.total × totalPercentUsed 伪造已用/总额文案。
