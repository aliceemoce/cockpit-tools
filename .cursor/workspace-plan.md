# 工作区计划

## 用户要求（权威）

1. 禁止猜测、假装工作、不使用工具、推论、编造；必须获得真实额度/可用性证据。
2. 要正确显示、正确上线、正确使用；点击账号打开后不能明明能用却显示没额度。
3. 需要知道哪些账号可以和 AI 对话；判断「可用」必须用该账号 CLI 真实对话验证。
4. 不以 usage-summary 冒充 Agent 可用性；限额 / 认证失败 / 网络失败分开显示。

## 当前阶段

Cursor 对话验活链路落地与验收（2026-07-17）。

## Pending

- [ ] 安装版部署验收（`npm run build` + `tauri build` 覆盖安装后 GUI 点验活）
- [x] 后端 Agent CLI 隔离对话验活并持久化 `chat_probe`
- [x] 前端「可对话 / 对话已限额 / …」展示；去除假 `17/59`
- [x] 用真实账号 CLI 验活：ok×2、rate_limited×1

## 已完成

- 2026-07-17：`cursor_chat_probe` 模块 + UI + 三账号实测落盘
