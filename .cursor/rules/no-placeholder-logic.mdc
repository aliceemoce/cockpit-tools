---
description: 禁占位符；不符规划必报错；禁止不报错；报错必须改；禁多余解说文本
alwaysApply: true
---

# 禁占位符 · 不符规划必报错 · 报错必须改 · 禁多余文本

## 范围

所有工作区 / 所有项目。

## 硬约束（三句）

1. **不符合规划必须报错**：缺数据、校验失败、API/任务失败、计划日不符等 → 必须可见硬报错（红条、exit≠0、日志原文等）。
2. **禁止不报错**：禁止静默吞错；禁止用占位符、假数据、软空态伪装成正常或成功。
3. **报错禁止不改**：硬报错是可见手段，不是交付；须修通内容直至错误消失、结果符合规划。仅贴错条/只改文案不修根因 = **未完成**。

## 禁占位符

- 禁止 `placeholder` 图、字块伪装（如「LoL/转载/赛程」灰块）、Demo/合成内容顶真结果。
- 失败/过期/错日不得当正常卡片。
- 表单 HTML 属性 `placeholder=`（输入提示）除外。
- 禁止用一种占位换另一种占位后宣称「去占位符已完成」（例：把哭脸改成「已删除」「下载中」字贴在封面位）。
- 禁止在封面位用文字、表情图标、加载点点顶替真实图片。
- **禁止**把「本地封面加载失败/路径不对」理解成「已删除」或「未下载」，更禁止因此删除列表里其它项或整项剔除（非用户原话）。
- 全局规则未写细 ≠ 允许做占位；工作区程序规划若被助手写进自编剔列表条款，须按用户纠正删掉。

## 禁多余解说文本

页面向用户只留：**导航名、业务数据、真实报错、必要表单标签**。

禁止向用户堆：设计原理、通道说明、「按计划日混排」、侧栏入口旁白、「执行由计划任务」、API 教学、cuid/android_id 派生教程、「按账号隔离」等架构表白文案。

## 硬阻塞

真硬阻塞（无出网、须扫码等）已尝试修通后可停，须写明阻塞事实；仍不得用占位符顶。
