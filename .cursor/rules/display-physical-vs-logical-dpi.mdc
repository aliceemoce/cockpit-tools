---
description: 显示尺寸须分清物理分辨率与逻辑像素，禁止把未 DPI 感知的测量当成物理屏外
alwaysApply: true
---

# 显示：物理分辨率 ≠ 逻辑像素

## 用户纠正（2026-07-30）— 仅用户原话

- 设置页：**缩放 150%**，**显示器分辨率 3840×2160**
- 质问助手说的「屏幕大小」是实测还是幻想

## 硬要求

1. 谈「屏幕多大 / 窗在不在屏内」前，须**当场实测**；禁止凭旧印象或未核对的脚本输出下结论。
2. **未做 Per-Monitor DPI 感知**时，`GetSystemMetrics` / 部分 `EnumDisplayMonitors` / 未感知进程里的截图 API 常返回**逻辑像素**（本机 4K@150% → 常误报 2560×1440）。**禁止**把逻辑桌面当成物理分辨率，再据此说「窗在屏外」。
3. 本机权威对照：物理 **3840×2160**，缩放 **150%**（DPI 144）；逻辑约 2560×1440 只是换算结果，不是另一块更小的物理屏。
4. 截图 / UIA 矩形：进程先 DPI 感知（或明确标注坐标空间是物理还是逻辑）；落点用 `scripts/_display_scale.py`（已要求感知后再量）。
5. 发现曾用逻辑像素误判屏外 → **同回合更正**，不得继续当硬阻塞交差。
