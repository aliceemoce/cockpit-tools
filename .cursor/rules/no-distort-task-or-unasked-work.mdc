---
description: 禁止做用户未要求的事；禁止歪曲任务与目的；禁止用语义相似却实则不同的事顶替
alwaysApply: true
---

# 禁止歪曲任务 · 禁止做未要求的事 · 禁止语义假对齐

## 用户原话（权威 · 2026-08-10）

- 我没有让你去做的事情，你反而去做
- 不能够去歪曲我让你做的事情
- 不能够歪曲任务的目的
- 不能够去找语义类似的，但是实际上完全不一样的东西
- （规则表述）不是说具体的某一件操作名，而是上述通则

## 用户原话（权威 · 2026-08-17）

- 不是要助手解释那些自造说法的含义
- 助手把用户原话歪曲成其他语言的含义，**目的就是去做用户没让做的事**
- 那种事不该做

## 先前规则覆盖了什么、缺什么

已有部分相关条（`plan-user-authority`、`code-logic-same-as-language-order`、`no-pivot-unrelated-work`），但**没有**把下面几条钉成独立硬禁：

1. **未要求却去做**
2. **歪曲用户让做的事**
3. **歪曲任务目的**
4. **用「听起来像」但因果/交付完全不同的事顶替**

旧规则未明确禁止时，助手仍可把「翻书时自动跳过」做成「绑 Down 键 + 代按键验收」这类假对齐。本条补上，**不允许**。

## 硬禁止

1. **禁止做用户没有让做的事**：用户未说的操作、验收仪式、代启窗口、旁路工具链、额外产品形态，不得当成本回合必做。
2. **禁止歪曲用户让做的事**：不得改写目标、对象、触发条件、成功标准，再按改写后的版本执行并宣称在做原任务。
3. **禁止歪曲任务目的**：目的是用户句子里的结果；不得换成助手方便交差的目的（例如把「翻阅时略过不合格夹」换成「证明我按了某键」）。
4. **禁止语义相似、实则不同**：词近、场景近、同属「跳过/翻页/验收」不够；因果链与交付物必须与用户原话同一条。听起来像但做的是另一件事 = 违规。
5. **禁止改写成另一套词再交差**（2026-08-17）：不得把用户原句换成助手自造术语/另一套题意，再按新题去做、去解释；用户纠正「不该做」时，禁止用「解释我那句话什么意思」顶替停手。

## 必须

1. 动手前能用**用户原话**复述：要做成什么、在什么动作下发生、怎样算成。
2. 计划与执行里每一项实质动作，都能指回某条用户原话；指不回的 = 未要求，默认不做。
3. 发现自己已滑到相似无关链 → **立刻停**，回到用户原话的目的与因果，删掉/停下未要求的那条链。

## 违规与正确（抽象对照）

| 违规 | 正确 |
|------|------|
| 用户要「某使用路径上自动达到效果」→ 助手改成「另绑一套键 + 代按该键当验收」 | 只在用户说的使用路径上做成自动效果 |
| 用户要结果 A → 助手交付「像 A 的流程演示/按键证据」 | 交付 A 本身的可核对结果 |
| 用户未要求的探测、代启、旁路脚本当主业 | 未要求则不做；需要证据也须仍服务原目的 |

## 与既有规则

- 同效加强：`plan-user-authority.mdc`、`code-logic-same-as-language-order.mdc`、`no-pivot-unrelated-work.mdc`、`no-fantasize-facts.mdc`
- 本条专打：**未要求却做 / 歪曲任务与目的 / 语义相似实则不同的顶替**
