---
description: Cockpit 特征注册表（能用/不能用/需要/不需要）+ 规则主动维护与 Plan 方针
alwaysApply: true
---

# Cockpit 特征注册表与规则演进

## 1. 四维分类（会话必读）

| 维度 | 含义 | Agent 行为 |
|------|------|------------|
| **能用** | 用户实测或日志/SHA/代码核实过的正确行为与 exe | 部署、验收、对比以此为锚 |
| **不能用** | 实测失败、用户拒收、或代码/日志证伪的版本与行为 | 禁止再当基线部署或宣称「已修复」 |
| **需要** | 用户明确要求保留或待实现的能力 | 改代码/编译前对照，不得删 |
| **不需要** | 用户明确拒收或未纳入 release 的能力 | 禁止擅自加回或当验收项 |

**优先级**：用户当场纠正 > 本规则注册表 > `project-brief.md` > `AGENTS.md` / `docs/CURSOR-FORK-SCOPE.md`（若冲突，**立即改注册表并同步旧文档**，不得沿用过期基线）。

---

## 2. 当前已核实特征（2026-06-22 上游 v0.26.5 合并后）

### 能用

| 项 | 证据 |
|----|------|
| **历史 Cursor 验收包** `%USERPROFILE%\Desktop\Cockpit-nirvana-token-test.exe` | SHA **`F5256511…`**，2026-06-15；**合并前** Cursor 链对照 |
| **合并后源码** | `integrate-upstream-v0.26.5-20260622` @ **`92cc6da0`**（含 upstream `d5ad5cea` + fork 切号/NVIDIA/Windsurf bootstrap） |
| **合并后调试构建** | `target/debug/cockpit-tools.exe` SHA **`50E61F27…`**，v0.26.5，`cargo build` + `npm run build` 通过 |
| 默认实例切号 | `close_cursor(仅默认 profile)`，**不全杀**其它 Cursor（源码仍在） |
| 多开 + 默认 **可同时存在** | 默认 `close_cursor`；多开 `close_cursor_profile_strict` |
| 配额 transient | 网络/超时失败 **不写盘**（`is_cursor_transient_quota_error`） |
| 满额换号 + Kh 裸 JWT | `pick_full_quota_account`、`nirvana_kh_auth_tokens` |
| `verify_cursor_switch_paths.py` | 2026-06-22 合并后 **PASS** |
| cliproxy xhigh + upstream URL 测试 | 2026-06-22 合并后 **PASS** |
| 历史备份 `9791D94F` | `Desktop\Cockpit-nirvana-token-20260614\` 仍在，作对照/回滚 |

### 不能用

| 项 | 原因 |
|----|------|
| **`CB525188…`** `scripts/_release_staging_r2/…r2.exe` | Agent 误标「6/15 r2」；非用户验收包 |
| **`94B7D986…`** / **`316B5F2A…`** 等 Agent 中间桌面包 | 未合 transient 或含 `7761f453` 回归 |
| **`7761f453`** / PR head `fork-on-upstream-0256` | 无条件写 `quota_query_last_error` + 全局 `accounts:changed` listener |
| **`9E0E625C…`** 强制轮换补丁版 | 用户拒收 |
| `%LocalAppData%\Cockpit Tools\cockpit-tools.exe` 6/9 旧构建 | 旧 `inject_to_cursor` 链 |
| 纯 **`eba93695`** 默认切号 | `close_cursor_nirvana_style` 全杀 Cursor |

### 需要

- 默认实例启动 **保留** 其它 Cursor 进程
- 无命令行闪屏
- 多开 profile 隔离 + 满额换号（非每次强制轮换）
- transient 失败不写盘；auth 类失败可写 `quota_query_last_error`
- **合并后**以 `integrate-upstream-v0.26.5-20260622` 为开发主线；release exe 待 GUI 点验后更新 SHA
- 线上对照：base = **`upstream/main` @ `d5ad5cea`**，head = **`integrate-upstream-v0.26.5-20260622`**

### 不需要

- 每次多开/全部启动 **强制轮换** 账号
- 默认实例 **taskkill 全部 Cursor**（`close_cursor_nirvana_style` 作默认 Play）
- 未说明就覆盖桌面 `Cockpit-nirvana-token-test.exe`
- `7761f453` 式全局 `accounts:changed` 监听
- 邮箱去重、额度池合并、scheduler/batch（见 `CURSOR-FORK-SCOPE.md`）

### 待修（代码层，非 exe 特征）

- 合并后 **GUI** Play/多开/NVIDIA 点验；release 打包（NSIS 下载失败时需重试）

---

## 3. 主动 Plan（任务开工前）

涉及 **编译、部署、切号、多开、验收 exe** 时，回复中须含简短 **Plan**（可嵌在 📋 项目理解后）：

1. 对照本节 **能用/不能用** — 目标 exe 的 SHA、路径、行为
2. 对照 **需要/不需要** — 本次改动是否触碰拒收项
3. 完成条件 — 日志字段 / 进程行为 / 脚本 JSON，写可观测标准
4. 若用户刚纠正过特征 — 本节是否已更新（未更新则先更新再动手）

复杂任务先 Plan 再改代码；用户仅问问题时 Plan 可一行带过。

---

## 4. 规则演进方针（发现即改，禁止只记脑子里）

### 必须写入或修改规则的情况

- 用户纠正：原「能用」变「不能用」，或反之
- 新 exe SHA / 路径 / 行为经磁盘或日志核实
- 新 **需要** 或 **不需要** 能力被用户明确
- 发现 `AGENTS.md`、`CURSOR-FORK-SCOPE.md`、`project-brief` 与实测矛盾
- 重复犯的 Agent 错误（版本搞错、混跑 exe、列表污染）

### 操作顺序

1. **先改** 本文件对应小节（能用/不能用/需要/不需要 + 日期）
2. **再改** 冲突文档（`AGENTS.md`、`docs/CURSOR-FORK-SCOPE.md`）一行指向或同步基线
3. **可选** 拆出专注规则（`.cursor/rules/*.mdc`），单条 <80 行；本文件保留索引
4. **`project-brief.md`** 本会话结论引用注册表，不重复编造版本故事
5. 重要架构决策：`agent-context` → `log_decision`（若 MCP 可用）

### 禁止（数据）

- **禁止**用 Python/脚本 HTTP 探测批量写入 `quota_query_last_error`（会误标「会话已过期」）；列表恢复只许从 **credentials 镜像 / Cockpit 自动备份** 还原字段，或让 Cockpit 自身刷新写盘
- `restore_cursor_quota_list.py` 已删除；误跑后用 `scripts/revert_restore_cursor_quota_list.py` 从镜像回滚
- 无 SHA/日志/代码证据就改「能用/不能用」
- 用户拒收后仍把同一特性标为「需要」或继续部署

---

## 5. 维护入口

- **主注册表**：本文件 §2（随纠正增量更新，注明日期）
- **会话快照**：`.cursor/project-brief.md`
- **release 边界**：`docs/CURSOR-FORK-SCOPE.md`（与 §2 冲突时以 §2 为准并回改文档）
