---
description: 每个工作区须维护目的、计划、程序设计三件套，并把程序规划写入 alwaysApply 工作区规则；开工前读取，缺失则创建
alwaysApply: true
---

# 工作区目的 / 计划 / 程序设计（三件套）

## 适用范围

**适用**：有明确项目根的工作区（含 `move_agent_to_root` 后、新建 git 项目、多文件代码库）。

**不适用**：Home 目录一次性问答/单条命令；用户明确说「不用写文档」；纯阅读/解释代码且不改文件。

## 三件套（工作区根目录 `.cursor/`）

| 文件 | 写什么 | 禁止写什么 |
|------|--------|------------|
| `workspace-purpose.md` | 工作区目标、为谁解决什么问题、范围边界、非目标 | 验收句式、助手流程、实现细节 |
| `workspace-plan.md` | 里程碑、待办、实施顺序；新需求**追加**，旧项标覆盖不删 | 架构细节、模块 API、可点/非空类验收 |
| `workspace-design.md` | 产品定位、子系统划分、模块职责、数据流、持久化、对外依赖 | 验收标准、探测步骤、截图留痕 |

已有**等价权威档**时不必重复造文件，须在会话中声明映射关系，例如：

- `AGENTS.md` + 根目录 `README` → 可部分替代 purpose/design
- `~/.cursor/plans/*主计划*.plan.md` → 可替代 plan
- 项目内专用索引或设计档、`*-plan.mdc` → 按项目规则为准

**缺映射且三件套任一缺失** → 视为未建档，须先补齐再改代码或跑破坏性 shell。

## 与 `user-history-requirements` 的分工

| 档 | 记什么 |
|----|--------|
| `user-history-requirements.md` | 用户当场要求、纠正、拒收、验收标准（随对话追加） |
| 三件套 | 工作区稳定意图与设计基线（随项目演进更新） |

冲突时：`user-history-requirements.mdc` 优先级更高。

**写三件套 / program-plan 时**：用户原话为权威，助手内容只能补充，细则见全局 `plan-user-authority.mdc`。

## Agent 硬约束

1. **开工前**：改代码、装依赖、写脚本、提交前，检查三件套或已声明的等价档是否存在且与当前任务一致。
2. **缺失时**：从 `README`、`AGENTS.md`、对话、代码结构归纳草稿，写入对应文件，**再继续** substantive 工作；不得无档硬改。
3. **变更时**：架构、scope、主流程变化后，同步更新 `workspace-design.md` 或 `workspace-plan.md`（带日期小节）。
4. **新建项目**：`create_project` 或首次迁入后，三件套至少各有一节骨架（目标一句、计划一条、设计一句模块划分）。
5. **只增不减（计划）**：`workspace-plan.md` 新需求追加；被取代的条目标「已被 YYYY-MM-DD 要求覆盖」，不删历史行。

## 各档最低内容

### workspace-purpose.md

- 一句话目标
- 用户/场景
- 在范围 / 不在范围（各 ≥1 条）

### workspace-plan.md

- 当前阶段
- Pending 待办（可勾选）
- 已完成（可选，保留历史）

### workspace-design.md

- 子系统或目录职责（表或列表）
- 关键数据流或状态（一句图或 bullet）
- 外部依赖（运行时、MCP、API，若有）

## 篇幅

单文件建议 ≤120 行；细节放项目 `docs/`，三件套只保留**可执行摘要**与链接。

## 程序规划（用户叫法）

用户说的「程序规划」= 本节的程序规划 / 工作区规划。细则见全局 alwaysApply `chengxu-guihua-from-user.mdc`：**须同时有「程序要求」（用户自然语言）与「代码实现」（具体文件落点）**、目的/程序要求等节禁英文套话、从用户原话提炼、自动化提示词从程序规划导出。

## 程序规划必须写入工作区规则（硬要求）

三件套 `.md` **不够**。须把当前**程序规划**（目的摘要 + 计划阶段/待办纲领 + 设计子系统与主数据流 + 本仓关键硬约束）写入工作区：

| 落点 | 要求 |
|------|------|
| `.cursor/rules/program-plan.mdc`（推荐名；可 `*-program-plan.mdc`） | `alwaysApply: true`；每轮会话强制进上下文 |
| 内容来源 | 同步自 `workspace-purpose.md` / `workspace-plan.md` / `workspace-design.md` 与主档中的**程序级**约束 |

### Agent 硬约束（本条）

1. **新建或首次迁入项目**：建三件套后**立刻**写/更新上述工作区规则；缺规则 = 建档未完成，不得改业务代码。
2. **规划变更后**：改三件套的同一回合内更新工作区 `program-plan.mdc`（或等价 alwaysApply 规则）；禁止只改 `.md`、规则仍是旧规划。
3. **开工前**：除读三件套外，须有可强制注入的工作区规划规则；没有则先补规则再干活。
4. **禁止**：用「规则里写了先去读 md」代替把规划正文写入 alwaysApply 工作区规则。

## 有档 ≠ 已做程序计划（2026-07-23 防偷懒）

细则见全局 alwaysApply `no-skip-program-plan.mdc`。摘要：

1. **`program-plan.mdc` / 三件套存在但与当前用户原话不一致 = 未做**；须同回合更新后再改代码。
2. **禁止等用户提醒**才补程序计划；用户因缺计划而追问 = 已违规。
3. **禁止用过时「当前权威任务」顶替**用户最新原话。
4. **会话 Plan**（`.cursor/plans/*.plan.md`）须含「程序计划落点」节，否则不得进 Agent 改业务代码。
5. 开工前须能指出：当前程序一句话目标 + 本回合待办已写入 `workspace-plan.md` 且镜像进 `program-plan.mdc`。

## 程序要求 vs 助手要求

程序定义写在 `workspace-design.md`（及 purpose/plan）并镜像进工作区 `program-plan.mdc`；验收方法与标准写在各工作区 `user-history-requirements.md` 或项目约定的助手要求档。细则见工作区 `program-vs-assistant-requirements.mdc`（若存在）或全局同类规则。
