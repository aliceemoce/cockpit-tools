---
description: 禁止因缺前置条件而放弃主任务、转做无关工作
alwaysApply: true
---

# 禁止偏离主任务转做无关工作

## 原则

用户指派的是任务 A，当前回合必须以完成 A 为目标。**不得**因本地缺文件、缺依赖、缺下载等原因，转而去做与 A 无关的 B、C。

## 禁止

- **任务漂移**：主任务未验收或未遇硬阻塞，却去写无关文档、改无关代码、探索无关目录、做「顺便优化」
- **以缺资源为借口**：「本地没有 / 还没下载 / 不在工作区」→ 跳过获取步骤，转做别的
- **自行降级 scope**：用户要完整结果，却只做 tangential 子集并当交付
- **假 busy**：用查资料、列清单、写计划代替真正执行主任务路径

## 前置条件缺失时

缺文件、缺包、缺 clone、缺模型权重等 **任务依赖** 时，按顺序：

1. **先获取**：下载、安装、克隆、解压到约定路径（大体积非刚需优先用户约定的大容量盘或工作区规则中的存储分层约定）
2. **再执行**主任务
3. **真卡死**（403、无权限、需用户扫码等）→ 只报硬阻塞事实，**停在该点**；不得转做无关工作

## 正确 vs 错误

| 违规 | 正确 |
|------|------|
| 用户要处理某游戏资源，本地无 → 去整理别的文件夹 | 先下载/定位资源路径，再按用户要求处理 |
| 缺模型权重 → 写一份「如何使用 ComfyUI」教程 | 尝试下载权重；失败则报 URL/错误码 |
| 编译缺依赖 → 重构无关模块 | 安装缺失包后继续编译 |

## 唯一例外

用户**明确**改口或取消 A、改做 B。

## 关联

`agent-execute-not-delegate.mdc`、`no-compromise-half-done.mdc`、`agent-output-constraints.mdc`
