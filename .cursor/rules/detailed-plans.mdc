---
description: 计划须尽可能详细：步骤多、少解释、规章具体、文字长、完全符合用户要求；实践测试为硬门禁
alwaysApply: true
---

# 计划尽可能详细

## 用户原话（权威 · 2026-08-04）

- 计划尽可能详细，步骤更多，少解释，规章具体，文字长，完全符合用户要求

## 覆盖旧条（当场纠正）

- **已被本条覆盖**：`self-plan-before-execute.mdc` 旧文「Plan 要短而可执行（步骤 ≤10 条为宜）」——**作废**，不得再按短计划缩 scope。
- 计划仍须先写「用户要求（权威）」再写「补充计划」；补充计划不得改写用户原话（见 `plan-user-authority.mdc`）。

## 用户纠正（权威 · 2026-08-09）— 计划与实践测试

- 质问：任务计划有没有任何「去实践测试」的要求；为何做成那种计划；全局规则怎么写的
- 质问：全局规则写得够不够规范、有没有给弹性余地；若有余地须把助手要做什么写清楚，且**不得局限某一工作区/某一程序**，须适用于所有工作情况

## 硬要求（写计划时）

1. **步骤更多**：把任务拆到可执行粒度（读什么、改什么、跑什么、留下什么证据、失败怎么换路径）。禁止三五条口号式步骤冒充计划。
2. **少解释**：计划正文写动作与落点，少写原理科普、背景铺垫、「为什么这样设计」。需要理由时一句贴用户原话即可。
3. **规章具体**：每步写清约束、禁做、验收数字/路径/日志关键字；禁止「妥善处理」「适当验证」「大致看一下」等空话。
4. **文字长**：以写全、写细为准，不为「简洁好看」删步骤；允许长计划、多节、多清单。
5. **完全符合用户要求**：用户原话里的每一条目标、边界、拒收、验收标准都必须出现在计划里并对上补充步骤；漏一条 = 计划不合格，不得进 Agent 改业务。

## 计划结构（最低 · 缺一不合格）

1. **用户要求（权威）** — 逐条原话或主档条目，不得合并歪曲
2. **补充计划** — 细步骤；每步对应上方某条用户要求
3. **落盘/同步（若本任务触及工作区规划或主档）** — 须写清是否已与本回合原话对齐；无则标 N/A 并一句理由
4. **实践测试（硬门禁）** — 见下一节；**禁止**用现象口号代替「助手本人要执行的动作」
5. **验收清单** — 可核对证据条目；每条须能对上「实践测试」里某一步的产出
6. **子代理分工（若适用）** — 哪些步并行派 `Task`、各自提示词要点、回报后主会话怎么收口；子代理结束 ≠ 任务结束

## 实践测试（硬门禁 · 适用于一切实质性交付）

凡任务会改行为、产出物、界面、服务、配置、数据，或用户要「能用/做成/修好」——计划里**必须**有独立节 **「实践测试（硬门禁）」**，且满足：

1. **写的是助手动作，不是产品说明书**  
   每条用「打开 / 运行 / 调用 / 点击 / 读取 / 比对」等动词，写清对象与预期客观结果。  
   **禁止**只写「用户应能看到…」「功能应正常…」「点 X 后应 Y」却不写谁去做、留下什么证据。

2. **与实现步骤同级，不得挤成收尾一句**  
   禁止把实践测试塞进「最后：编译覆盖并验证」这类单行附言。须可单独勾选、可单独失败。

3. **未跑通实践测试 = 任务未结束**  
   实践测试任一条未留下硬证据 → 不得勾完成、不得宣称已验收。硬阻塞（须用户物理键、扫码、UAC 等）须写明阻塞点与已尝试的替代路径。

4. **证据形态（按交付类型选用，至少一类写死在计划里）**  
   - 有界面：本人打开目标 → 进程/窗口硬证据（交互会话、标题非空、主窗口句柄非 0，适用时）→ 对目标控件或等价遥控完成操作 → **截图落盘 → 助手本人打开截图看一眼并据图写结论**（禁止只报接口 JSON 冒充已看见界面）  
   - 无界面（库、脚本、服务、批处理）：本人运行主路径命令或接口 → 留下完整退出码与关键 stdout/日志原文（或约定的成功结构）  
   - 有「有/无输入」两态时：计划须写清两态各怎么测，禁止只测快乐路径

5. **禁止用下列顶替实践测试**  
   仅编译通过；仅改文件落盘；仅版本号升高；仅清单/报表行数；仅口头「应该可以」；把打开与核对推给用户；**有界面却不截图或不看图**。

## 禁止（计划弹性封死）

- 为「短」砍步骤、砍验收、砍用户边界、砍实践测试
- 大段解释、小段动作
- 计划与用户原话不一致仍开干
- 用「大致流程」代替逐步规章
- **验收清单只有「应有现象」、没有「助手必做动作 + 证据」** —— 视为未写实践测试 = 计划不合格
- 把「自验证」写成可选、顺带、有空再做

## 与既有规则

- 权威顺序：`plan-user-authority.mdc`
- 工作流：`self-plan-before-execute.mdc`（Plan→Agent 仍要走完；**计划篇幅与步数以本条为准**）
- 打开与验证执行态：`agent-self-open-and-verify.mdc`（本条管**计划必须写进实践测试**；那条管**执行时必须亲自做**）
- 子代理：`active-subagents.mdc`、`subagent-end-not-task-end.mdc`
