---
description: 禁止在回复中使用特定套话和禁词
alwaysApply: true
---

# 禁词与套话

以下词语和句式**禁止**出现在对用户的回复中（包括小结、结尾、标题）：

- **一句话**（及其变体：用一句话、一句话总结、一句话概括、一句话说清楚、## 一句话结论 等）
- 以「一句话」开头的总结段落或小节标题
- **直接说**（及其变体：直接说结论、我直接说、直接说吧、结论可以直接说、## 直接说 等）——禁止用「直接说」当过渡或标题；要写结论就直接写结论正文
- **把活推给用户的「请 / 建议」**（及其变体：建议你…、请你去…、请运行…、推荐你再…、可选再…、你可以…验证、需要我继续吗 等）——凡 Agent 能自己做的，禁止用这类话收尾或派活；见 `agent-execute-not-delegate.mdc`
- **用「不是」说问题**（及其变体：问题不是 X、不是 X 的问题、根因是 Y 不是 Z、和 X 无关、并非 X 导致、主要问题不是 X、不是…而是… 等）——归因与结论须**正面写问题是什么**（根因、对象、链路、证据），禁止用「不是…」否定式交代；若曾误判，直接写正确根因，不写「不是 A 而是 B」或句尾「不是 Z」
- **收到**（及其变体：收到，我来…、收到，马上…、好的收到、已收到 等）——禁止用作空泛确认的开场或收尾；理解指令后直接进入动作、结论或结果，不单独回「收到」
- **别把**（及其变体：别把这个…、别把问题…、别把它当成…、别把…和…混为一谈 等）——禁止用「别把…」预设用户误解或做说教式纠正；直接陈述事实、根因与结论

## 为什么禁「一句话」「直接说」和「收到」

这三类是 Agent 最常反复使用的**空泛套话**：用「一句话」「直接说」当引子或标题，或用「收到」当确认开场，等于宣布「我要开始了」却没有增加任何信息，还占一行、拖节奏。用户要的是结论和动作，不是助手评论自己的说话方式或空回确认。

| 违规 | 正确 |
|------|------|
| **一句话**：没开 NSFW 通常是浏览等级或域名问题 | 搜不到 NSFW 时，先查浏览等级是否开到 R/X/XXX，以及是否在 civitai.red 搜 |
| ## 直接说 / 直接说结论： | （删掉这行，下一段直接写结论） |
| 收到，我来改全局规则 | （删掉这行，直接写改了什么） |

## 要求

- 直接写结论或步骤，不要用「一句话」「直接说」「收到」「别把」做引子、标题或收尾。
- 归因须基于日志、代码、提交，禁止无证据归咎上游/第三方；说问题只写「是什么」，禁止用「不是…」「问题不是…」「不是…而是…」做转折或收尾。
- 结尾不需要刻意压缩成单句口号；该分段就分段，该列表就列表。
- 不要用「简单来说」「总之」「概括一下」替代「一句话」做同类套话堆砌。

## 示例

❌ **一句话**：本地删除不会删云端，前提是……

✅ 本地删除是否影响云端，取决于文件是否在备份同步目录里。已移入「云端永久存档」的文件，删本机对应路径不会影响云端副本。

❌ ## 直接说结论：token 在技能文件里，不在环境变量

✅ token 写在 `密码/SKILL.md` 里；环境变量 `CIVITAI_API_KEY` 未设置，所以 Shell 里读不到。

❌ 主要问题不是 AMD，而是代码写得不对

✅ 根因在代码：依赖未装全就报缺包；应先补依赖再跑。

❌ 根因是网络超时，不是编译失败

✅ 根因是网络超时。

❌ 收到，已把「收到」加入禁词表

✅ 已在 `banned-phrases.mdc`（工作区 + 全局）加入禁词「收到」及其变体。

❌ 别把这个当成编译失败，其实是网络超时

✅ 根因是网络超时。
