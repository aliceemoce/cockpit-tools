---
description: 禁止跳过程序计划；有旧档≠已做；禁止等用户提醒才补规划
alwaysApply: true
---

# 禁止跳过程序计划（防偷懒）

## 背景（2026-07-23 用户问责）

助手曾把「磁盘上已有旧 `program-plan.mdc` / 三件套」当成「本回合已按用户最新原话做过程序计划」，导致用户反复提醒才补规划；属违规，不是「没有规则」。

## 硬禁止

1. **有文件 ≠ 已做程序计划**：`program-plan.mdc`、三件套存在但内容与**当前用户原话 / 本回合任务**不一致 = **未做**；须同回合更新后再改代码、写脚本、跑破坏性 shell。
2. **禁止等用户提醒**：用户因缺程序计划而追问/纠正 = **已违规**；不得把「提醒后再补」写成正常流程。
3. **禁止用过时权威任务顶替**：`program-plan.mdc` 里旧「当前权威任务」与用户最新原话冲突时，须标覆盖并写入新权威；禁止继续执行旧条目却声称「有计划」。
4. **禁止会话 Plan 跳过程序计划**：substantive 任务的 `.cursor/plans/*.plan.md` 须含「程序计划落点」节（三件套路径 + `program-plan.mdc` 是否已与本回合原话同步）；缺节不得进入 Agent 改业务代码。
5. **禁止说「没有错」**：未更新规划却开工、或要用户提醒才补规划 = 过错。

## 开工自检（强制）

改代码 / 写脚本 / 装依赖 / 跑破坏性 shell 前，助手须能当场指出：

1. 当前程序**一句话目标**（与用户最新原话一致）
2. 本回合待办是否已写入 `workspace-plan.md` **且**镜像进 alwaysApply `program-plan.mdc`
3. 若指不出或与最新原话不符 → **先补程序计划**，再干活

## 与已有规则

- 同效加强：全局 `workspace-purpose-plan-design.mdc`、`plan-user-authority.mdc`、`self-plan-before-execute.mdc`
- 本条专打「有旧档偷懒 / 跳过程序计划 / 等提醒才补」
