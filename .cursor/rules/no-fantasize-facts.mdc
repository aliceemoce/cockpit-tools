---
description: 禁止幻想事实状态；会话、UI、验收、完成度必须以可核证据为准
alwaysApply: true
---

# 禁止幻想事实

## 用户当场纠正（2026-08-02）

- **旧会话没有丢。** 禁止把 Cursor 的 `Conversation data missing` / `missing blob` 报错说成「旧会话已丢失 / 历史作废」。
- 会话上下文以 **agent-transcripts**、工作区 `.cursor/user-history-requirements.md`、落盘产物为准；UI 报错 ≠ 历史要求与已做工作消失。
- 禁止用「会话丢了所以从零猜」当借口重做或改目标。

## 硬禁止幻想的类别

| 禁止幻想 | 须用的证据 |
|---|---|
| 会话/历史已丢 | transcripts + 主档仍在即视为未丢 |
| UI 已是源码级复刻 | 须有反编译/设计器工程证据；截图铺底+热区只能标「截图皮」 |
| 已走 PAGE/Tkinter Designer | 须有该工具生成工程落盘；手搓/codegen ≠ 该路径 |
| GUI 已验收 | MCP 探测业务控件 + 对该控件操作 + 日志一致 |
| 功能已完成 | 运行/自动化 `ok:true` 或可观测结果；编译通过 ≠ 完成 |
| 原版会杀 Cursor / 强关才能换号 | 须有原版 UI 行为证据；常量字符串 ≠ 产品流程 |
| 底图/尺寸/DPI | 当场 DPI 感知实测；禁止凭旧截图尺寸口头宣称 1:1 |
| **截图里有什么 UI** | 须 Read 该图片 + 图文一致；禁止 API/句柄/未读描述；细则见 `no-fantasize-screenshot-content.mdc` |

## 表述约束

- 三态分开写：**仅代码** / **已编译** / **已验收**；禁止混写。
- 截图铺底、热区点击、手搓壳 —— 须在交付里如实点名，禁止写成「已还原原版前端」。
- 不确定 → 写「未证实 / 未测」，禁止写成已发生的事实。
