---
description: 禁止为加新功能破坏原程序规格与能力；禁止平行叠层抢入口；通则适用于一切程序
alwaysApply: true
---

# 禁止破坏原功能换新功能 · 贴合宿主规格

## 用户纠正（权威）

### 2026-08-12

- 你好像很喜欢删除功能
- 需要把禁止删除功能写在规则里

### 2026-08-13

- 做的**根本不是原本程序规格**的东西 → 是**乱做**
- 乱做会导致**各种冲突**
- 禁止这么做；要做**符合它自带逻辑、不冲突**的做法
- 规则须**指导以后更多思考**，而不是乱做
- 不要拿鸡毛蒜皮的细节辩解；**从大局上看**

### 2026-08-14

- 总爱改一个程序的原本的功能，破坏原本功能来增加新功能
- 要能在全局规则里禁止这么做
- 问：当前改的这条升全局后，有没有部分程序不适用；现有有没有同类规则

## 大局（通用于一切宿主程序）

凡已有完整规格的程序（内置命令、键位、设置持久化、钩子、API、插件点等）：

- **正确** = 在该程序**自带规格链**上达成用户目标；新能力是加法或规格内扩展
- **错误** = 为交差砍掉/架空原能力，或在规格外再叠一套平行机制，和原生抢同一入口

**冲突与丢功能是架构问题，不是某个参数写错。**

## 硬禁止

1. **禁止破坏原功能换新功能**：加跳过、换键、换滚动、换排序、换入口时，禁止顺带让用户原本能用的能力失效（含静默解绑、清空原生命令、改成另一套抢同一热键）。
2. **禁止平行叠层**：同一用户动作禁止「宿主一套 + 助手脚本/旁路一套」双轨并存抢同一入口。
3. **禁止热路径反复写配置**：禁止启动/打开/每次操作都重写用户设置、键位、参数并依赖退出落盘——与用户手改持续打架。
4. **禁止未对照规格就打补丁**：未先回答「官方谁负责、能否规格内一次定稿、谁是唯一主人、会否冲突」不得改键位/入口/持久化。
5. **禁止用改数据冒充程序能力**：改名、改时间戳、改缓存、挪文件让界面「看起来对」≠ 程序做成（见 `no-fake-ideal-by-mutating-workspace.mdc`）。

## 动手前必过（缺一项不得改宿主设置/入口）

1. **自带规格里谁负责**这个行为？
2. **能否只用规格内手段**一次定稿？
3. **一条能力一个主人**——最终由谁执行？
4. **冲突预检**——会否和内置命令、已有扩展、用户手改打架？
5. **计划写清**自带路径；若必须旁路，旁路只做哪一段、不碰哪些原入口。

## 适用边界（回答「会不会部分程序不适用」）

| 适用 | 说明 |
|---|---|
| **通则始终适用** | 不破坏原能力、不平行抢入口、不热路径反复写配置——一切仓 |
| **细则例子可换** | 具体命令名、设置文件名、钩子名随宿主变；不得因「这不是某某程序」就整条作废 |
| **唯一例外** | 用户**当场**明确要求删减 scope、或计划写明「原生无法实现」且用户要该因果时，才允许旁路；旁路仍不得顺带砍无关原能力 |

## 与现有规则分工

| 规则 | 管什么 |
|---|---|
| `no-delete-existing-features.mdc` | 修一点时不得删其它已有功能；加法或等价替换 |
| **本条** | 加新功能时不得破坏原规格/原入口；禁止平行叠层与配置打架 |
| 各仓专用（如 NeeView `native-spec-no-parallel-stack.mdc`） | 把本通则落到该程序的命令/设置/钩子名 |

## 违规 vs 正确

| 违规 | 正确 |
|---|---|
| 为脚本清空原生换书/滚动键 | 原生能做的走原生；脚本只补原生没有的那段 |
| 加跳过时把空格/排序/其它键改坏 | 只改跳过因果链；其余能力行为不变并复验 |
| 功能不通再叠第三套入口 | 先查规格内为何不通，修到单轨可验收 |
