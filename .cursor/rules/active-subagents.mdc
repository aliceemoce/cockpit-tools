---
description: 积极使用子代理；可同时运行复数子代理
alwaysApply: true
---

# 积极使用子代理 · 可并行复数

## 用户原话（权威 · 2026-08-04）

- 积极使用子代理
- 可以同时运行复数子代理

## 硬要求

1. **默认积极用 `Task` 子代理**：凡探索大范围代码库、并行调研、独立多步子任务、壳命令批处理、对照审查等，优先派子代理，禁止主会话独自串行硬啃全部可拆工作。
2. **可同时运行复数**：彼此无强依赖的子任务，必须在**同一条消息里并行**发起多个 `Task`（多块 tool call），禁止故意串行「一个做完再开下一个」拖长墙钟时间。
3. **选型要对口**：按任务选 `explore` / `shell` / `generalPurpose` / `ci-investigator` / `best-of-n-runner` 等；提示词须自含全部上下文（子代理看不到父会话原文）。
4. **主会话仍负责验收与收口**：派出去不等于交差；子代理回报后主会话须核对证据、合并结果、继续未完项。见 `subagent-end-not-task-end.mdc`。

## 必须并行的典型场景

| 场景 | 做法 |
|---|---|
| 多目录/多模块摸底 | 多个 `explore` 同时开 |
| 改 A 同时查 B 的依赖/用法 | 实现与检索分派并行 |
| 多失败项互不依赖 | 按失败项各派一个子代理 |
| 构建/测试与只读排查可重叠 | `shell` 与 `explore` 并行 |

## 禁止

- 能拆成独立子任务却全程只在主会话里慢慢搜、慢慢读
- 可并行时写成「先等第一个子代理结束再开第二个」
- 子代理提示词缺路径、缺验收、缺用户原话约束，导致空转或偏题
- 把「开了子代理」当成任务已完成（见下一条规则）

## 与既有规则

- 同效加强：`no-one-step-then-stop.mdc`、`agent-execute-not-delegate.mdc`、`no-compromise-half-done.mdc`
- 计划细节：`detailed-plans.mdc`
- 结束边界：`subagent-end-not-task-end.mdc`
