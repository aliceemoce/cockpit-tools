---
description: 积极使用子代理；可同时运行复数子代理；断后续做仍走子代理
alwaysApply: true
---

# 积极使用子代理 · 可并行复数

## 用户原话（权威 · 2026-08-04）

- 积极使用子代理
- 可以同时运行复数子代理

## 用户原话（权威 · 2026-09-06）

- 我看对子代理不是使用很积极，怎么回事
- 我看子代理断了就倾向于主对话做了，这个还是不行，子代理断了额度恢复也是子代理继续

## 硬要求

1. **默认积极用子代理**：凡探索大范围代码库、并行调研、独立多步子任务、壳命令批处理、对照审查等，优先派子代理，禁止主会话独自串行硬啃全部可拆工作。
2. **可拆就派**：本回合存在可拆子任务时必须派子代理；可拆数为零才允许主会话自干；有一项可拆却未派 = 违规。
3. **可同时运行复数**：彼此无强依赖的子任务，必须在同一条消息里并行发起多个子代理，禁止故意串行「一个做完再开下一个」拖长墙钟时间。
4. **选型要对口**：按任务选探索 / 壳命令 / 通用 / 持续集成排查 / 多方案试跑等；提示词须自含全部上下文（子代理看不到父会话原文）。
5. **主会话只验收与再派**：派出去不等于交差；子代理回报后主会话须核对证据、合并结果；未完的业务实现须再派或续做子代理，禁止主会话接手该子任务干活。见 `subagent-end-not-task-end.mdc`、`subagent-resume-not-main.mdc`。
6. **开工自检**：须能答本回合派了几个子代理、为何未派（仅无可拆时）。

## 必须并行的典型场景

| 场景 | 做法 |
|---|---|
| 多目录/多模块摸底 | 多个探索子代理同时开 |
| 改甲同时查乙的依赖/用法 | 实现与检索分派并行 |
| 多失败项互不依赖 | 按失败项各派一个子代理 |
| 构建/测试与只读排查可重叠 | 壳命令与探索并行 |

## 禁止

- 能拆成独立子任务却全程只在主会话里慢慢搜、慢慢读
- 可并行时写成「先等第一个子代理结束再开第二个」
- 子代理提示词缺路径、缺验收、缺用户原话约束，导致空转或偏题
- 把「开了子代理」当成任务已完成（见 `subagent-end-not-task-end.mdc`）
- 子代理断了或额度打断后，主会话把该子任务自己做完（见 `subagent-resume-not-main.mdc`）

## 与既有规则

- 同效加强：`no-one-step-then-stop.mdc`、`agent-execute-not-delegate.mdc`、`no-compromise-half-done.mdc`
- 计划细节：`detailed-plans.mdc`
- 结束边界：`subagent-end-not-task-end.mdc`
- 断后续做：`subagent-resume-not-main.mdc`
