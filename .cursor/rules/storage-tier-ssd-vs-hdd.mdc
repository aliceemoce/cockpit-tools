---
description: 非SSD刚需内容优先放H盘
alwaysApply: true
---

# 存储分层约定

- 有明确 SSD 性能刚需的内容（系统盘依赖、编译缓存、频繁随机读写的活跃项目）放在 SSD。
- 不属于 SSD 刚需的下载文件、归档数据、大体积素材、历史输出等，优先放在 H 盘（移动 HDD，容量优先）。
- 若无更明确要求，非 SSD 刚需下载目录默认使用 `H:\Downloads`。
- 若任务明确指定了其他路径，以用户明确指定为准。
