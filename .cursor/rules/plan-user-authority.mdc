---
description: 计划模式与工作区规划：用户原话为权威，助手计划只能补充
alwaysApply: true
---

# 计划以用户原话为准

## 权威顺序

1. **用户当场原话**（要求、约束、边界、验收标准）= 唯一权威
2. **历史要求主档**（`.cursor/user-history-requirements.md`）= 持久化的用户原话
3. **助手写的计划** = 仅补充（步骤拆解、风险、验收落点），**不得**覆盖或改写 1/2

## 适用场景

- **Plan 模式**（`SwitchMode(plan)` 写的会话内计划）
- **工作区三件套**（`workspace-purpose.md` / `workspace-plan.md` / `workspace-design.md`）
- **工作区 program-plan 规则**（`program-plan.mdc` 等 alwaysApply 规划）
- **制作 / 更新 Cursor 规则**（含 `.cursor/rules/*.mdc`、全局 `~/.cursor/rules/*.mdc`）时的规划与落盘
- **计划任务 / 日课槽位**规划（如 `schedule.json`、cron 安排）

## 写规则时的硬门禁

1. 「用户纠正 / 用户原话 / 用户要求」节**只能**写用户当场说过或主档已记的原话（可摘要，不得歪曲）。
2. 助手从截图、日志、实现需要**推断**出的说法或做法 → 只能写在「补充 / 实现」节，**禁止**标成用户纠正；禁止发明用户未使用过的词当正式要求名。
3. `create-rule` 技能里的「从对话推断规则」**不得**压过本条：推断 ≠ 用户原话；未说的验收/禁令不得当 alwaysApply 硬要求。
4. 发现规则里已有非用户原话冒充用户纠正 → **同回合改掉**，不得用「规则里已经写了」辩护。
5. **程序规划 / 工作区规划**（三件套、`program-plan.mdc`、本仓与全局 alwaysApply）必须以用户**实际命令**为绝对前提；禁止把手编造句写进规划当作用户授权。专条：`no-invent-user-speech-in-plans.mdc`。
6. 禁止在「用户原话」节用助手括号（「（拒收）」「（钉死）」等）冒充用户说过。

## 必须

- 计划开头须有 **「用户要求（权威）」** 节，逐条写入用户原话或主档对应条目（可摘要，不得歪曲）
- 助手的步骤、风险、验收写在 **「补充计划」** 节，且每条须对应上方某条用户要求
- 用户未说的 scope、行为、验收标准 **不得**自行加入当作硬要求
- 执行中若发现助手计划与用户原话冲突 → **以用户原话为准**，改计划再继续
- 落盘「用户原话」前须能指回当场原句或主档自动同步原文；指不回 = 不得写入

## 禁止

- 用助手自己的计划结构、习惯、推断 **替代** 用户表述
- 计划里写与用户原话矛盾的目标、范围、验收（如用户说「只猫吧」计划写「全关注吧」）
- 漏写用户当场强调的限制，只写助手认为「合理」的方案
- 把用户原话压成一句带过，却用大段助手自拟的「目标/架构」占主体
- 把助手推断写进规则并标成「用户纠正 / 用户原话」
- **编造用户原话写入程序规划 / 工作区规划**（点名行为；2026-08-22）

## 验收

计划合格 = 用户要求节完整可读 + 补充计划不越权 + 无冲突项 + 无假「用户原话」；不合格不得进入 Agent 执行或落盘三件套。
