---
description: 禁止幻想截图/UI 可见内容；须亲读截图且图文一致；API/句柄≠看见界面
alwaysApply: true
---

# 禁止幻想截图与界面可见内容

## 用户原话（权威 · 2026-08-11）

- 停止幻想截图内容
- 停止你的幻想，完成你的测试
- 全局规则如果没有禁止你幻想你就加上

## 硬禁止

1. **禁止未读就描述截图**：本回合要对某张截图下结论时，须先用 Read 打开该图片文件；未读不得写「截图里可见…」「红框/高亮/某页/某控件已在图上」。
2. **禁止凭 API/日志/句柄冒充已看见界面**：遥控 `ok:true`、`highlightedGid`、进程句柄、窗口标题 ≠ 已看见目标 UI。接口成功只算**后端/遥控层**；**界面验收**须截图 + 亲读 + 图文一致。
3. **禁止图文不一致仍标验收通过**：亲读截图后，若看不见口头声称的控件、文案、红框、高亮、滚动位置 → 写**未通过**，继续修或换截法；不得改口硬圆、不得用「应该已…」交差。
4. **禁止错窗/错屏当目标证据**：全屏 `ImageGrab`、未绑定目标 hwnd/进程/标题的截图，若内容不是目标应用（例：游戏、浏览器、别的 IDE）→ 该文件**作废**，不得写入验收结论；须换 `PrintWindow(目标句柄)` 或 UIA `capture_screenshot` 等可核对方法重截。
5. **禁止用「应有现象」代替「图上现象」**：计划/规格里写了红框、定位标签、滚到传速项，不等于截图里已有；只认**图上实际像素**。
6. **禁止把 image description 当亲读替代品**：工具返回的图片描述仅供参考；写验收结论前仍须 Read 该图片；描述与亲读不一致时，以**亲读**为准，并如实写「描述不可信/截错窗」。

## 必须

1. **截图 → 读图 → 结论**：顺序不可跳过；结论只写亲读能指认的内容（窗口标题区、顶栏文案、列表/网格项、高亮样式等）。
2. **分离三态表述**：
   - **遥控/API**：贴 JSON 或日志原文；
   - **截图**：写路径 + 亲读看见什么；
   - **验收**：仅当两者与任务要求一致时可写通过；不一致须写「API 过 / 界面未过」或反之。
3. **失败如实写**：截图里没有红框、没有「正在传 · 已定位」、仍在已完成区/错误分组 → 验收**失败**，不得写「已完成跳转验收」。
4. **窗口截图最低核对**：桌面 GUI 截图须能指认进程/标题与目标一致（句柄、PrintWindow 尺寸合理，非 158×26 等异常条）；异常则重截，不得用该图验收。

## 与已有规则

- 同效加强：`agent-self-open-and-verify.mdc`（截图+看图）、`no-fantasize-window-use-mcp.mdc`、`no-fantasize-facts.mdc`、`strict-no-execution-hallucination.mdc`
- 本条专打：**幻想截图里有什么 / 没看图就描述 / API 代替亲读**
