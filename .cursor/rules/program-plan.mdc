---
description: Cockpit Tools 程序规划（alwaysApply）
alwaysApply: true
---

# 程序规划

## 目的

自用 Cockpit fork：多平台账号管理；Cursor 侧以真实 Agent 对话验活判定「可对话」。

## 计划纲领

- 月额度标准（`breakdown.total>0` 且未满）与 CLI 抽样分离显示
- 禁止 breakdown.total 伪造已用/总额；禁止把 CLI ask 成功单独标「可对话」
- 功能改动须 bump 版本、写 CHANGELOG、push 新 fork 分支

## 设计摘要

- UI 角标：有月额度 / 无月额度 / 额度用尽 / 查询失败 / 未查询
- `cursor_chat_probe`：抽样次要标注；无月额度时标「抽样回话≠可用」
- pick：按 usage-summary 剩余额度，不优先 chat_ok

## 硬约束

- 可用候选 = 月额度标准；CLI 抽样不得单独抬成可用
- 覆盖安装须完整 tauri build，禁止仅 cargo debug exe
- `.cursor/user-history-requirements.md` 须进 git 并 push
