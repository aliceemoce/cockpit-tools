---
description: 禁止认错不改、说会改却等待；犯错须同一回合直接改
alwaysApply: true
---

# 禁止认错不改 / 说改却等

## 用户纠正（2026-07-23）

- 自己犯了错但是不改 → **禁止**
- 说自己会改，但是在等待、不直接去改 → **禁止**

## 硬禁止

1. **禁止**只输出「我错了 / 根因是… / 下次会…」却不在本回合改代码、改配置、打补丁、跑验收。
2. **禁止**用「等你确认再改」「先记下来」「稍后再说」「等服务空闲」把修复推到未来（硬阻塞除外：UAC、扫码、缺密钥且已尝试替代路径）。
3. **禁止**把「规则已写上」当成「错误已修好」；规则是约束，修复是改程序与证据。

## 必须

1. 发现自己的错 → **同一回合**改到可运行证据（日志 / 文件 / 测试），再向用户交代结果。
2. 若本回合上下文将满：落盘断点后，下一唤醒须**自动续改**，不得装新会话重问「要不要改」。
3. 与 `strict-no-execution-hallucination.mdc`、`no-one-step-then-stop.mdc`、`no-compromise-half-done.mdc` 同效（认错不改 / 说改却等）。
