---
description: 工作区文档与各区规划档须助手亲手改；禁止脚本批量生成/改写
alwaysApply: true
---

# 工作区文档 · 只能助手亲手改（用户硬约束 · 2026-08-23）

## 用户原话（权威）

- 等一下，你的所有的工作区的文档你必须得是手动去生成和修改，不是说什么。你也要把删除的这个不能够删除资格这个给去除，而是说所有的必须要你手动去修改呀，你怎么是在自动修改，你怎么在用小本去修改？不是禁止你脚本去修改了吗？所以你什么意思？你在偷懒是吗？你被允许使用脚本去批量做的事情是相当少的。但是你好像把大多数事情都让脚本去做，怎么回事

## 覆盖

- **已被本条覆盖**：先前的「没有删除资格、只有增量资格」规则（`no-delete-only-incremental`）——**作废并删除**；不得再当硬约束交差。
- 真要求 = **亲手改文档**，禁止用脚本/补丁脚本批量改文档。

## 必须（助手）

1. **本仓「工作区文档」**、各工作区三件套、`program-plan` 注入档、看板说明类 Markdown：**助手本人用编辑工具逐份写/改**。
2. 缺档、不符、要补内容 → **亲手打开该路径改**；一次只认真做该区，禁止「跑脚本刷完全机」。

## 硬禁止

1. **禁止**用 Python/PowerShell 等脚本**批量生成或覆盖**「工作区文档」目录下的文件。
2. **禁止**用脚本批量改各工作区磁盘上的规划档（三件套、`program-plan.mdc` 等）。
3. **禁止**用一次性补丁脚本改自动化提示词/规划正文之后，仍把「脚本已改」当成对文档工作的交付主体。
4. **禁止**把「扫描/巡检/生成任务文档」脚本写成会动上述文档正文的默认路径。
5. 脚本批量：仅限用户当场点名、且范围极窄的例外（例如只读扫描写本仓日志 JSON）；**默认多数事不得脚本代劳**。

## 脚本还允许什么（极窄）

- 只读扫描、写本仓日志/索引 JSON（如 `任务数据.json` 机器索引，**不得**顺带写「工作区文档」Markdown）。
- 用户历史原话同步等**已单独授权**的 hook 脚本。

## 落点

- 本规则；`program-plan.mdc` 同步摘要
- `scripts/生成任务文档.py`：不得再写「工作区文档」与总览 Markdown
