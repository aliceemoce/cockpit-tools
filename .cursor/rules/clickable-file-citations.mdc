---
description: 助手引用磁盘文件须用可点击代码块，禁止报错路径
alwaysApply: true
---

# 可点击文件引用

## 必须

向用户指向**工作区内**已有文件时，只用 Cursor 代码引用块：

```起始行:结束行:工作区相对路径
```

- 路径相对当前工作区根，用 `/`，禁 `c:\` 绝对路径
- 指整文件：读文件后给真实行号范围（可 `1:末行`）
- 禁 markdown 链接、`[text](file://…)`、URL 编码路径

## 禁止

- 反引号包绝对路径当「可点链接」
- 表格/正文裸写 `C:\Users\…` 冒充可打开引用

## 验收

用户点击引用须在编辑器打开对应文件行，不得 `Unable to resolve resource`。
