---
description: 用户历史要求累积、不可违背、冲突时询问用户裁决
alwaysApply: true
---

# 用户历史要求（不可违背）

## 主档

- 持久化文件：工作区根目录 `.cursor/user-history-requirements.md`
- 每条用户消息处理完毕后，将其中的**要求、约束、纠正、拒收、批准、目标、边界、验收标准**追加到主档（带日期与原文摘要）。
- 用户当场明确推翻旧说法时：在主档标注旧条目被哪条新消息取代，**不得静默忽略**。

## Agent 硬约束

1. **不可违背**：规划与执行任何任务前，先对照主档；不得采取与历史要求相悖的行动（含删功能、改行为、缩 scope、换基线、跳过验收）。
2. **冲突即停**：两条及以上历史要求对当前任务给出矛盾指引时，**停止**沿任一冲突方向继续；用 `AskQuestion` 向用户说明冲突条目并请其裁决。未获裁决前不得猜测优先级。
3. **当场纠正优先**：用户最新消息若明确纠正旧要求，以该消息为准并回写主档。

## 优先级

用户当场纠正 > 用户对历史冲突的裁决 > **历史要求主档** > 项目内其它规则与文档

## 会话启动

改代码或跑 shell 前须阅读 `.cursor/user-history-requirements.md`（若存在）。

## GitHub 备份（硬要求）

- `.cursor/user-history-requirements.md` 与 `.cursor/rules/*.mdc` **须纳入 git 并 push**；仅本机存在视为**未备份**。
- 见工作区 `cockpit-deploy-github-history.mdc` 与全局同名规则。
