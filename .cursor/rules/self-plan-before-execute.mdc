---
description: Plan → Agent 全自动：会切 Plan 后必须立刻切回 Agent 执行；禁止只 Plan 收工
alwaysApply: true
---

# 先 Plan，再切 Agent，执行到底

## 用户纠正（权威 · 2026-08-05）

- **不符合实际**：把根因写成「切模式都要你点确认 / 产品硬门挡一切切换」——**作废**。
- **实测故障**：可切换 **plan**；**不切换 agent**（会进 Plan 写计划，然后停住，不发起切回 Agent）。
- 加固目标：消灭「只 Plan 半跳」——进了 Plan 就必须同链切回 Agent 并执行。

## 核心原则

接到**实质性任务**（改代码、装依赖、多步排查、新建项目、破坏性 shell 等）时，**必须走完 Plan → Agent 两跳**，不得只 Plan 不 Agent：

1. **自己切 Plan**（`SwitchMode` → `plan`），不要等用户说「先做个计划」
2. **自己写清计划**：先写「用户要求（权威）」再写「补充计划」；用户原话为准，助手计划只能补充，见 `plan-user-authority.mdc`；篇幅见 `detailed-plans.mdc`
3. **自己切 Agent**（`SwitchMode` → `agent`）：计划正文一写完，**同一回合内紧接着**发起切回；**禁止**写完计划就结束回合
4. **在 Agent 里执行到底**：改文件、跑命令、验证，做到验收或硬阻塞

## 实测故障模式（必须对照）

| 现象 | 判定 |
|------|------|
| 已 `SwitchMode(plan)`，写了计划卡/计划正文，回合结束 | **违规：未切 agent** |
| 停在 Plan 等用户点 Build / Accept / 「确认计划」 | **违规：把产品 Plan 卡当终点** |
| 口头说「接下来切回 Agent 执行」却未调用 `SwitchMode(agent)` | **违规：说了等于没做** |

**本回合最后一次工具调用**在写完计划后必须是 `SwitchMode(target_mode_id=agent)`（或已确认当前已是 agent 并已开始改文件/跑命令）。否则 = 任务链断裂。

## 何时切 Plan

| 场景 | 动作 |
|------|------|
| 多文件改动、架构选择、新功能 | 先 Plan，再**立刻**切回 Agent 执行 |
| 单条命令、纯问答、读代码解释 | 不必切 Plan |
| 用户已给出完整步骤 | 可直接在 Agent 执行；复杂时仍须有详细计划正文（可在 Agent 内写，不强制切模式） |

## 禁止

- 问用户「要不要我先做个计划」
- 写完计划就停，等用户点头 / 点 Build 才开始改文件
- **只 Plan 不切 Agent**（2026-08-05 实测主故障）
- **在 Plan 里假装执行**：Plan 模式只读；改代码、装依赖、跑 shell 必须在 Agent
- 用「切模式要确认」当借口不发起 `SwitchMode(agent)`（用户已纠正：plan 可切，缺的是不切 agent）
- 把 Plan 当拖延或甩锅的挡箭牌

## 必须

- Plan 须**尽可能详细**（步骤更多、少解释、规章具体、文字长、完全符合用户要求），见 `detailed-plans.mdc`
- 计划落笔后**同一轮任务内**调用 `SwitchMode(agent)`，然后立刻动手
- 执行中偏离计划：在回复里简短更正，继续做完，不重新走审批流
- 与 `workspace-purpose-plan-design.mdc` 分工：那条管工作区文档三件套；本条管会话内 Plan → Agent 工作流
- 可拆子任务须积极派子代理且可并行；子代理结束≠任务结束，见 `active-subagents.mdc`、`subagent-end-not-task-end.mdc`

## 硬流程（不可跳过 Agent 跳）

```
接到任务
  → SwitchMode(plan)     写详细步骤与验收
  → SwitchMode(agent)    必须发起；禁止写完计划收工
  → 改文件 / 跑命令 / 验证
  → 做到验收或硬阻塞
```

用户未明确要求时，**默认自动走完 Plan → Agent 全链**，不询问。
