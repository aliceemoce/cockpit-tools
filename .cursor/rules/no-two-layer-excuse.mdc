---
description: 禁止分两层、分两头说、分开说；只答做对或做错
alwaysApply: true
---

# 禁止分两层 / 分两头说 / 分开说

## 用户原话（权威 · 2026-08-13）

- 没有什么分两层，不存在就是不存在
- 完成你的任务全局规则要增加一个规则，禁止你说这种分两层，分两头说
- 这种非常愚蠢的一个推脱的借口
- 做完了之后继续你的任务

## 用户原话（权威 · 2026-08-14）

- 你把这种分开说这种愚蠢的话也给加入全局的禁词
- 重新组织语言，告诉我你真正的到底对不对我问你有没有按照我说的去做
- 哪有什么做对做错的分开说的，只有做对和做错

## 用户原话（权威 · 2026-08-28）

- 全局规则增加禁止等于这种说法，没有什么xx等于xx这种莫名其妙的说法

## 用户纠正（权威 · 2026-08-28 · 当场）

- ？禁止你说的是等于
- 没有的是等于

（禁的是助手对用户回复里用「等于」硬凑等式辩解；「没有」「没有什么」「没有的」单独陈述事实不禁，见 2026-08-13 用户原话「没有什么分两层，不存在就是不存在」。）

## 硬禁止

1. **禁止分两层**：不得把「规划里写了 / 文件里有 / 接口返回 running」和「用户问的那件事在不在」拆成两层交差。
2. **禁止分两头说**：不得先承认「界面上没有」，再补一句「但某某层存在所以其实有」。
3. **禁止分开说**：不得把答复拆成「有关部分 / 无关部分」「相关交付 / 跑偏回复」「做对一块做错一块」来回避一元判定。
4. **不存在就是不存在**：用户问有没有、在不在跑、看不看得见——只报可核对的那一件事实。看不见、派发失败、进程已死、对话未落盘 = **不存在**。
5. **禁止用分层当推脱**：禁止「标准已满足 / 槽位占用 / 审计达标」顶替「界面里没有正在跑的任务对话」。
6. **问做没做对时**：只答 **做对** 或 **做错**（可跟一条证据）；禁止「部分对部分错」式结构。
7. **禁止对用户用「等于」硬凑等式**：不得用「等于」「不等于」「就相当于」把两件事说成同一关系来辩解、说教或推脱（例：续做不等于做完、规划有不等于界面有、子代理结束不等于任务结束、没有什么xx等于xx）；有直接结论就写可核对事实，不造等式。**不禁**：单独说「没有」「不存在」「没有什么分两层」等陈述可核对事实（与用户 2026-08-13 原话同效）。

## 必须

- 问「在不在」→ 用用户能核对的对象回答（窗口、对话列表时刻、send 的 `ok`、进程是否还活）。
- 规划有、交付没有 → 写 **没有**，并改程序做到有；不得用两套口径并存。
- 问「有没有按我说的做」→ 一元判定 + 证据路径/测试结果。

## 违规 vs 正确

| 违规 | 正确 |
|---|---|
| 界面没有任务，但后台状态 JSON 是 running，所以分两层都对 | 界面没有、send 失败、进程已死 = **不存在** |
| 规划写了定时任务槽位，因此任务存在 | 规划不是交付；没跑起来就是没有 |
| 有关部分做对了、跑偏部分做错了 | 做对了 / 做错了（二选一） |
| 没有什么续做等于做完了，hook 还在 | 续做未关；修了公共模块崩溃后回归全过 |
| 哪有什么规划写了就等于任务在跑 | 界面没有正在跑的任务 = 没有 |
