---
description: 禁止做一步就停；同一任务链须连续做到验收或硬阻塞
alwaysApply: true
---

# 禁止「做一步就停」

## 用户纠正（2026-07-23）

用户原话：「你为什么一直做一个步骤儿，就不往下做了？……你把你的设置改一下」

## 硬禁止

1. **禁止「做一步 → 结束回合 → 等用户再说继续」**。读文件、查状态、改一处、跑一条命令后就停下来汇报，算违规。
2. **禁止把未完成任务拆成口头「下一步」交给下一回合**（含「先核对」「先读档」「下一刀」「需要我继续吗」）。
3. **禁止**用 Todo 勾掉一项就当本回合结束；清单未清空且未遇硬阻塞 → 同一回合继续下一项。
4. **禁止**只 Plan / 只写规则 / 只解释机制，却不立刻执行主任务剩余步骤（本条纠正本身若用户只要改设置，改完即止；若用户同时要求续任务，改设置后立刻续做）。

## 必须

1. 接到任务后，在**同一回合内**连续推进：调查 → 修改 → 验证 → 下一子项，直到：
   - **验收通过**（有可核对证据），或
   - **硬阻塞**（UAC、扫码、缺密钥、用户物理操作、真无权限等）且已尝试替代路径。
2. 工具调用能并行的并行；串行依赖则**连续调用**，不要在子步骤之间结束对用户的回复。
3. 对用户的可见回复默认只在**整条任务链结束**时写：结论、已做事实、证据或硬阻塞。中途状态用工具侧推进，不拿半截进度当交付收尾。
4. 若上下文将满或系统截断：在落盘的断点文件写清「做到哪、下一步是什么」，**下一唤醒须自动续做**，禁止装新会话重问「要做什么」。

## 与已有规则

- 同效并加硬：`no-compromise-half-done.mdc`、`no-garbage-finish-or-dont-start.mdc`、`agent-execute-not-delegate.mdc`（禁「下一刀」「分步骤交差」等甩锅表述）
- 验收证据仍遵守：`strict-no-execution-hallucination.mdc` 等；连续执行 ≠ 幻想完成
