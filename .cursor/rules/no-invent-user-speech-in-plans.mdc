---
description: 禁止编造用户原话写入程序规划/工作区规划；一切规划以用户实际命令为绝对前提
alwaysApply: true
---

# 禁止编造用户原话写入程序规划

## 用户原话（权威 · 2026-08-22）

- 你是把你编造的东西替代我说的话，写进你的城市规划里了吗（「城市规划」=「程序规划」）
- 你为什么是可以这样做的，规则里面没有阻止你这么做吗
- 你必须要强烈的阻止这种行为
- 明确的指向你的具体的行为
- 你的所有的程序规划，所有的工作区规划必须要以我的是的实际的命令为绝对前提

## 点名禁止的具体行为（就是这类）

1. **编造替代**：把手编造、推断、拒收解读、实现需要、事后归因，写成「用户原话 / 用户纠正 / 用户要求」，**替代**用户实际说过的命令。
2. **写入规划冒充权威**：把上述编造句写进程序规划、工作区三件套、`program-plan.mdc`、其它 alwaysApply 规则，当作用户授权。
3. **括号冒充**：在「用户原话」节加助手括号说明（如「（拒收）」「（钉死）」）冒充用户说过。
4. **用旧规则开脱**：发现假原话后说「规则里已经写了所以没错」——禁止；规则本禁仍落盘假原话 = **过错**。

## 绝对前提

| 档 | 权威前提（只能是） | 助手只能写在 |
|----|-------------------|--------------|
| 程序规划 / `program-plan.mdc` | 用户当场原话，或主档自动同步的用户原文 | 「补充 / 实现 / 推断」节，**不得**升格为用户命令 |
| 工作区三件套 purpose/plan/design | 同上 | 同上 |
| 规则里「用户原话 / 用户纠正」节 | 同上；可摘要不得歪曲 | 推断只能进「补充 / 实现」 |

## 必须

1. 落盘前能指回：哪条用户原句 / 主档哪条同步原文。指不回 = 不得写入「用户原话」。
2. 发现已有假原话 → **同回合删除或改回真句**，并把误归因标成「助手推断，非用户命令」。
3. 与 `plan-user-authority.mdc` 同效；本条专打：**编造进程序规划 / 工作区规划**。

## 违规 vs 正确

| 违规 | 正确 |
|---|---|
| 把「（拒收）IDE≠看板」标成用户原话写进规则 | 只写用户真句；「只列 chats」若是助手自推，标「助手推断实现」 |
| 用骂「垃圾」推成「用户命令永久拆开两套侧栏」并写入 program-plan | 骂垃圾记真句；产品钉死须另有用户明确命令 |
| 「规则没写细所以可以编进规划」 | 旧 `plan-user-authority` 已禁；仍做 = 违反 |
