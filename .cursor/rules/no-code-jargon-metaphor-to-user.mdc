---
description: 对用户说话禁止代码、自造词、比喻；只说做了什么、结果是什么
alwaysApply: true
---

# 对用户说话：禁止代码、自造词、比喻

## 用户原话（权威 · 2026-08-19）

- 全局规则里禁止你跟我说话的时候使用代码。使用你的制造的词，使用你莫名其妙的比喻。你要务实的告诉我，你到底在说什么，做什么。谁知道你在比喻什么？谁知道你在用你说的自定义的代码是什么含义？说这些垃圾话干什么

## 硬禁止（对用户可见回复）

1. **禁止贴代码**：禁止函数名、字段名、文件名当正文、反引号包一串标识、哈希、编号串、命令原文堆在回复里。用户当场明确要看某段程序时除外。
2. **禁止自造词**：禁止助手发明用户没说过的叫法当正文（例如把「核对账号还能不能对话」说成自造黑话）。
3. **禁止比喻**：禁止用踢、链、层、探活、写穿这类比喻顶替事实。
4. **禁止用标识当解释**：用户看不懂的内部名字，不得当结论。

## 必须

对用户只写日常中文：做了什么、结果是什么、还成不成。界面上的字可以原样引用（那是屏幕上的字，不是助手自造）。

## 违规 vs 正确

| 违规 | 正确 |
|---|---|
| 贴一串内部名字和数字当交差 | 额度提示打断后，自动又发了「继续做」；我接着核对了账号还能对话 |
| 「踢一脚」「写穿探活」 | 系统自动续做了；我核对了当前登录号还能对话 |
| 用自造词代替「弹窗还在不在」 | 屏幕上「你已达到用量上限」这块：找到了 / 没找到 |

## 与既有规则

对用户说话以本条为准。改程序、跑命令仍在工具里做，只是回复里不许拿那些内部名字交差。
