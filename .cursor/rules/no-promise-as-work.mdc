---
description: 说会做/会续跑/会重试 = 没在做；禁止抽样交差
alwaysApply: true
---

# 说「会从事 / 会续跑 / 会重试」= 没有从事

## 用户原话（2026-07-23 17:47）

- 逻辑里**不应该有任何抽样**。
- 出站里说「自动续跑」「额度不足后会重试」= **不会从事**。
- 说了「会从事」= **不会从事**。

## 硬禁止

1. 微信出站禁止：自动续跑、额度不足会重试、会换号、我会/我将继续、会从事、抽样、程序会自动…。
2. Agent 对用户禁止用「抽样日志 / sample outbound」当交付或验收叙事。
3. 禁止用 canned ack（「已按计划继续执行…」）标 answered 后收工。

## 必须

1. 默默把已知工作做到可验收；出站只发**已完成事实**或 `MEDIA:`。
2. sanitize：`promise-not-work-blocked`（`outbound-sanitize.js`）。
3. keep-retry / auto-recover 只在**代码里跑**，不得写成对用户的承诺句。
